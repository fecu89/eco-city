# Progress Log — AI 시티를 구하라!

## 원본 요청

> "지금 AI_City_Webgame_V3에 웹앱을 만들어놨어. game creator의 하네스를 보고, 이 게임을 완성시켜줘. 웹 앱의 형태였으면 해."
> (이후) "다 해줘. 그리고 단계를 6단계까지 늘렸으면 해. 교육과 함께 게임성도 추가해줘. 우리의 원래 계획도 첨부해봐. 이 게임이 우리의 계획과 적합한지, 충분히 교육적인지도 봐줘."
> (이후 정정) "여기서는 발표까지 구현하는게 아니라 수업중에 이용할 게임 그 자체를 개발하는게 목표야. 6번은 게임에서 빠지는거임. 게임성을 더 추가하자는거지."

배경: 통합과학 교사(변석환, 신장고)가 실제 4차시 수업(성취기준 10통과2-02-05/06, 10통과2-03-03/04)에 쓸 목적으로 만든 교육용 시뮬레이션. 지도안 PDF 2개(`docs/lesson-plan-source.md`, `docs/common-framework-source.md`) 참고.

## 세션 결정 사항

- 빌드 도구: **Vite로 전환** (CDN 제거, npm 번들링)
- AI 어드바이저: **결정형(규칙 기반) 유지**, 실시간 LLM API 연동 없음
- 배포: **이번 세션은 범위 밖** (로컬 완성까지)
- 범위: **게임 내 플레이(1~5단계 + 선택적 6단계 보너스)만 구현**. 지도안 4차시(발표+투표+헌장)는 오프라인 교실 활동 — 게임에 넣지 않음
- 승인된 하드닝 항목 7개 전체 반영 (재시작/터치타겟/자동저장/오디오/퀴즈확장/완성도/교사용 리포트) — 자세한 내용은 `docs/gameplan.md`

> **2026-09-02 주의**: 이 절과 아래 gotcha가 가리키는 `docs/gameplan.md`의 "6단계 설계"·"하드닝 항목 7개"·"3단계(뇌-AI 에너지 비교)" 섹션은 그날 gameplan.md를 현재 구현 기준으로 다시 쓰면서 사라졌다. 옛 6단계 설계 요약과 무엇이 언제 왜 빠졌는지는 `docs/gameplan.md`의 `## 설계 변천`에 있고, 뇌-AI 에너지 출처 링크 3개도 같은 절의 `### 보관 — 뇌-AI 에너지 비교 출처`로 옮겼다. 이 아래 세션 기록은 당시 그대로 두었다.

## 원본 프로토타입

`_legacy-v3/`에 원본 3개 파일(index.html/script.js/style.css) 백업 보존. 게임플레이 설계(4단계, 10개 시설, 인접 시너지, 8개 배지)는 이미 완성도 높았고 그대로 승계 — 문제는 하네스 아키텍처 부재(EventBus/GameState/Constants/테스트 전무)였음.

## TODO (구현 순서) — 전부 완료

- [x] 지도안/공통틀 원문 보관 + gameplan.md/tech.md/ADR 작성
- [x] Vite 스캐폴딩 (package.json, vite.config.js, npm 의존성 설치 — three/animejs@3.2.2/chart.js/lucide)
- [x] core/ (Constants.js, EventBus.js, GameState.js) — 기존 데이터 이식 + 신규 스테이지/에너지비교/대화록 필드
- [x] systems/ (Board/Achievement/Advisor/Stage/Crisis/Concepts/Diagnosis/Redesign/Report/Save)
- [x] ui/ (Grid/Dock/Panels/Modal/StageModals/Toast/Chart/ThreeBackground/DiagnosisView/MobileNav/FeedbackBridge)
- [x] audio/ (SFX 7종 세분화 + 선택적 배경음 토글)
- [x] main.js 오케스트레이터 + render_game_to_text + advanceTime
- [x] index.html/style.css 신규 UI(진단 스캐너, 에너지 저울, AI 말대로 짓기, 보너스 라운드) 반영
- [x] Playwright QA 스위트 (tests/e2e/game·visual·perf·mobile.spec.js, 19개 테스트)
- [x] npm run build / test 검증 + Playwright로 전체 플레이스루 확인 (1→6단계, 리셋 3회, 새로고침 자동복구)
- [x] 최종 리포트

## 검증 중 발견하고 고친 버그

- **토스트 무한루프 (심각)**: 토스트 개수 제한(`MAX_VISIBLE_TOASTS`) 로직이 애니메이션(비동기)으로 오래된 토스트를 제거하려 해서, 토스트가 빠르게 3개 이상 쌓이면 `while` 루프가 끝나지 않고 렌더러가 멈춤(Playwright "Target crashed"로 확인). 힌트 버튼을 빠르게 여러 번 누르면 실제로 재현됨 — 학생이 버튼을 연타하는 흔한 상황이라 실사용에서도 발생했을 것. `src/ui/ToastView.js`에서 초과분을 동기적으로 즉시 제거하도록 수정. `tests/e2e/game.spec.js`의 "does not crash on rapid toasts"가 회귀 테스트.
- 진단 힌트 버튼이 힌트 소진 후에도 계속 클릭 가능해 "더 이상 힌트가 없습니다" 토스트가 반복 발생 — 힌트 소진 시 버튼을 disabled 처리하도록 수정.
- 배지가 8→9개로 늘었는데 배지 그리드가 4×2(8칸)로 고정돼 9번째가 잘릴 수 있었음 — 3×3 그리드로 수정.
- **lucide 아이콘 전체가 안 보이던 버그 (2차 피드백)**: `createIcons({icons: ICONS})`에 kebab-case 키(`'circle-help'`)를 넣었는데, lucide 내부의 `replaceElement()`는 `data-lucide` 값을 PascalCase로 변환(`toPascalCase("circle-help") → "CircleHelp"`)해서 `icons[ComponentName]`으로 찾는다. 키가 안 맞아 전부 `undefined` → `console.warn`만 뜨고(에러 아님) 아이콘이 조용히 안 보이는 상태가 됨. "콘솔 에러 없음" 테스트로는 못 잡아서 별도로 lucide 관련 `console.warn`을 감시하는 회귀 테스트를 추가함. **키는 반드시 PascalCase.**
- **토스트 카운트 제한의 while 루프 재발 + 로딩 화면 CSS 트랜지션 타이밍**: 3D 보드 테스트 중 발견. `#loadingScreen.done`은 `opacity`/`visibility`를 `transition:.4s`로 바꾸는데, `visibility: hidden`으로 가는 트랜지션은 스펙상 **트랜지션이 끝나야** 실제로 hidden이 적용된다. 즉 JS가 `.done` 클래스를 붙인 시점과 화면이 실제로 클릭을 더 이상 가로채지 않는 시점 사이에 ~400ms 간극이 있다. 테스트 픽스처가 이걸 감안 안 해서 레이캐스팅 클릭이 로딩 화면(`#loadingText`)에 막혀 씹히는 것으로 오인될 뻔했음 — 실제 앱 버그는 아니고 테스트 픽스처(`tests/fixtures/game-test.js`)의 대기 시간을 늘려 해결. 사람이 쓸 땐 로딩 애니메이션 자체가 짧아 체감되지 않는 수준.

## 2차 피드백 (사용자가 직접 플레이 후) — 전부 반영

- 우상단 아이콘 안 보임 → 위 lucide 버그 수정
- 데이터센터 설명 텍스트가 잘 안 보임 → 독(dock) 설명 텍스트 제거, 대신 독에서 시설 선택 시 보드 위에 **인접 보너스(초록)/갈등(빨강) 구역을 실시간 미리보기**로 표시 (`BoardSystem.placementPreview`, 전 시설 공통)
- 인접관계가 +만 있고 -가 없음 → 원전-주거지(불안), 데이터센터-주거지(소음·발열), 화력·공장-녹지(환경 훼손) 갈등 규칙 3종 신규 추가 (`getCellSpatial`/`calcMetrics`), SSI(과학기술 사회적 쟁점) 성취기준과도 연결됨
- 근거 입력 칸이 너무 작음 → 사이드바 폼 제거, 시설 검사 모달에 "근거 기록" 버튼 추가해 넓은 전용 모달(`openEvidenceEntryModal`)로 이동
- 도시를 3D로 보고 싶음 → **보드 전체를 Three.js 3D 씬으로 교체** (`src/ui/CityScene3D.js`). 레이캐스팅으로 클릭을 칸 인덱스로 환산, OrbitControls로 드래그 회전/휠 줌(기본은 보기 좋은 고정 각도, 회전은 선택사항 — 모바일에서 필수 제스처 아님). 일반 보드(`GridView`)와 진단 스캐너(`DiagnosisView`) 둘 다 같은 3D 씬을 공유하며 클릭 핸들러만 바꿔 낀다.
- "사각형만 올리면 어떡해"(3차 피드백) → 처음엔 시설 색상만 다른 단순 박스였는데, 시설 타입별로 **기본 도형을 조합한 실루엣**으로 교체(`BUILDING_FACTORIES` in `CityScene3D.js`): 주거지=집(박스+지붕), 공장=몸체+굴뚝 2개, 데이터센터=박스+발광 안테나 4개, 화력=박스+높은 굴뚝+붉은 불빛, 원전=냉각탑 실루엣(테이퍼 원기둥)+돔, 태양광=기울어진 패널, 풍력=타워+**실제로 도는 날개**, 에너지저장=배터리 모양, 순환냉각=탱크+물방울, 녹지=미니 나무 3그루 군집. 레벨(1~3)은 부위 비율을 유지한 채 전체 크기만 키운다. 외부 3D 모델(GLB) 대신 기본 도형 조합을 쓴 이유: 학교 네트워크 의존성 없음, 번들 크기 유지, 라이선스 이슈 없음, 스타일 일관성.

## 4차 피드백 — 3D 조작·객체·성능 전면 개선

원본 요청: “3D 화면은 이동이 안 돼 드래그조차 안 됨”, “성능이 너무 떨어짐”, “assets/city-kit을 보고 레벨별로 색상을 다르게”. 사용자가 제안안 전체 적용과 막히는 부분의 권장안 선택을 승인함.

- [x] `CameraController` 분리: 마우스 드래그 회전, 우클릭 팬, 휠 줌, 터치 1손 회전/2손 이동·확대, 보드별 거리·극각·팬 경계, 44px 초기화 버튼.
- [x] `public/assets/city-kit`에 시설 주 모델 10개+보조 2개+texture/license 선별(18파일, 약 1.6MB). 비동기 GLB 정규화·캐시와 시설별 절차형 폴백 추가.
- [x] 레벨 시각 언어 확정: Lv.1 회색/0.86배/1 segment, Lv.2 파랑/1배/2 segments, Lv.3 주황/1.13배/3 segments. 빨강은 경고 전용.
- [x] 셀별 Mesh 재생성을 영구 `InstancedMesh` 레이어로 교체. 타일 레이캐스팅은 단일 instanced tile의 `instanceId`를 사용.
- [x] 배치 480ms, 업그레이드 520ms, 철거 320ms 전환. 풍력 rotor와 데이터센터·화력·냉각 particles는 30fps 공유 레이어.
- [x] 거대한 원형 상태 링을 코너 브래킷으로 교체해 객체 가독성 개선. 모바일 보드가 viewport 밖으로 넘치던 CSS 폭 버그도 회귀 테스트와 함께 수정.
- [x] 장식용 두 번째 Three.js/WebGL/RAF 제거 → CSS 배경. DPR 상한(데스크톱 1.5/모바일 1.25), GPU buffer prewarm, dirty rendering 적용.
- [x] QA를 44개로 확장: 카메라/에셋/모션/단일 context/24 draw-call 예산/0 buffer churn/idle draw/mobile touch/6종 visual snapshot.

### 최종 성능 실측 (1440×900, Chromium, DPR 2 입력)

- 6×6 혼합 도시 36시설: **21 draw calls** (예산 24), 실제 pixel ratio 1.5.
- WebGL context: **2 → 1**.
- warm-up 뒤 30회 UI redraw: WebGL buffer create/delete **0 / 0** (개선 전 같은 유형 측정 360 / 360).
- 빈 도시 안정 상태 500ms: 추가 WebGL render **0회**.
- 빌드 산출물 JS: 약 941KB, gzip 약 270KB. City Kit 정적 에셋은 별도 약 1.6MB이며 로딩 실패 시 게임을 막지 않고 폴백한다.

## 5차 피드백 — 전체 화면 도시 월드 HUD

원본 요청: “건축물을 짓는 대지 자체가 무척 큰 배경”, “canvas 이외의 항목은 버튼으로 숨기기”, “top-actions/main-buttons는 설정 모달”, “시장보좌관·성취도 별도 버튼”, “도시 상태 반투명 float”, “facility-dock 숨기기”, “모바일 하단바”.

- [x] 기존 2열 대시보드를 제거하고 Three.js 도시 캔버스를 뷰포트 전체 고정 월드로 확장.
- [x] 우측 `CITY CONTROL` 레일에 건설/AI/도시/성취/메뉴 5개 기능을 배치하고 한 번에 한 패널만 열리도록 `WorldHud.js` 상태기계 추가.
- [x] 건설은 반복 배치가 가능한 하단 플로팅 팔레트로 유지. 시설 선택 뒤에도 열려 있고 선택 시설·비용·조작 안내를 갱신.
- [x] 시장 보좌관은 우측 drawer, 도시 상태는 작은 반투명 radar 계기판, 배지/근거는 탭 drawer로 이동.
- [x] 도움말/음악/효과음/초기화/AI 조언/단계 진행 버튼을 `menuPanel`로 물리적으로 이동하되 기존 id와 게임 이벤트를 유지.
- [x] 단계/도움말/시설 검사 모달이 열리면 HUD를 닫고 잠그는 우선순위 추가. Escape/바깥 클릭/같은 버튼/닫기 버튼과 포커스 복귀, `aria-expanded`/`aria-controls`/`aria-hidden`, reduced-motion 대응.
- [x] 모바일은 5개 44px 하단바와 최대 56dvh 하단 시트로 교체. 닫은 뒤 실제 한 손 터치 회전이 즉시 복귀하며 시설 카드는 66px 가로 스크롤로 전체 이름을 표시.
- [x] 데스크톱 기본/건설/상태/메뉴와 모바일 기본/건설 시트 스냅샷을 직접 검토하고 시각 기준으로 고정.

### 최종 검증 (2026-08-29)

- `npm run build`: 성공. JS 943.53KB(gzip 270.75KB), CSS 38.11KB(gzip 8.35KB). 기존 500KB chunk 경고만 유지.
- `npm test -- --reporter=line --retries=0`: **55/55 통과** (2 workers, 2.1분).
- 전체 화면 캔버스: 데스크톱 viewport 폭/높이 각각 95% 이상 회귀 테스트 통과.
- 30회 HUD 패널 전환: WebGL buffer create/delete **0 / 0**.
- 기존 3D 기준 유지: WebGL context 1개, 6×6 혼합 도시 21 draw calls(예산 24), warm redraw buffer 0/0, idle render 정지.
- 사용자 지시에 따라 **git push, PR 생성, 배포는 수행하지 않음**. 모든 변경과 검증은 로컬 작업공간에서만 진행.

## 6차 피드백 — 게임플레이 피드백·테마·생활 도시

- [x] 건설 팔레트가 열린 상태만 건설 모드로 정의. 팔레트가 닫히면 빈 대지를 눌러도 건설하지 않으며, 실제 캔버스 `pointerdown`이 팔레트를 먼저 닫던 입력 순서 버그도 수정.
- [x] 다음 단계 조건 달성 순간 우선 토스트와 메뉴 unread 표시. 단계 변경/메뉴 확인 시 해제.
- [x] 메뉴의 중복 AI 조언 제거. 시장 보좌관 패널이 AI 질문/자동 건설을 단독 소유.
- [x] 다크/라이트 CSS+Three.js 통합 스키마, 메뉴 전환, `localStorage` 저장. 라이트 전환 시 기존 타일 instance color까지 즉시 갱신.
- [x] 성취마다 상단 배너, radial burst, 우선 토스트, 효과음, 성취 unread 표시. 성취 패널 확인 시 해제.
- [x] 모바일 5버튼 하단바와 HUD 계약을 유지한 채 신규 알림/테마를 동일하게 적용.

## 7차 피드백 — ambient 성능 우선 축소

원본 요청: “프레임이 너무 자주 가고 객체가 많다”, “차라리 선을 하나 깔고 5초에 한번씩 깜박이면 충분”.

- [x] 연결별 이동 에너지 패킷을 전부 제거(`energyPacketCount = 0`).
- [x] 데이터센터·화력·냉각의 연속 `Points` 파티클 레이어 제거.
- [x] 모든 에너지 연결을 공유 `LineSegments` 하나로 유지하고 5초마다 180ms 밝힘/복원만 수행.
- [x] 사람·차·새와 풍력 날개는 공유 인스턴스의 정적 소품으로 전환. 정착 도시의 30fps ambient 렌더 제거.
- [x] 대표 6×6 도시 1초 관측에서 추가 렌더 최대 2회(점멸 밝힘/복원), 패킷 0개, 5초 점멸 1회 회귀 테스트 추가.
- [x] 다크/라이트/모바일 시각 스냅샷을 단순화된 장면으로 다시 생성하고 직접 검토.

최종 전체 검증 결과는 아래 최신 검증 섹션에 기록한다.

## 최신 검증 (2026-08-30)

- `npm run build`: 성공. JS 950.73KB(gzip 273.55KB), CSS 42.74KB(gzip 9.51KB). 기존 Vite 500KB 청크 경고만 유지.
- `npm test -- --reporter=line --retries=0`: **67/67 통과** (2 workers, 2.3분, 재시도 없음).
- 에너지 성능: 이동 패킷 0개, 공유 전력선 1 레이어, 5초당 점멸 1회. 정착된 대표 6×6 도시는 1초 관측 시 추가 렌더 최대 2회 계약 통과.
- 3D 성능 계약: WebGL context 1개, 대표 6×6 도시 24 draw calls 이하, 30회 redraw와 30회 HUD 전환의 WebGL buffer create/delete 0/0.
- 시각 회귀: 다크/라이트, 성취 해금, 데스크톱/모바일을 포함한 13개 스냅샷 통과 및 직접 검토.
- 실제 입력 회귀: 건설 팔레트를 연 뒤 캔버스 중앙 좌표 클릭이 index 12에 주거지를 배치하는 테스트 통과.
- 저장소는 일반 checkout의 `main`이며 사용자가 소유하지 않은 저장소라는 지시에 따라 **commit, push, PR, 배포를 전혀 수행하지 않고 로컬 변경 상태로 보존**.

## 8차 피드백 — 건설 가능 상태와 배치 표시 동기화

- [x] 시설 비용이 현재 크레딧보다 크면 건설 버튼을 실제 `disabled` 처리하고 흐리게 표시.
- [x] 비활성 시설의 툴팁에 필요한 크레딧 부족분을 표시(예: `1C 부족`).
- [x] 선택 시설은 유지하되 건설 패널이 닫히면 배치 이득/갈등 미리보기를 즉시 제거.
- [x] 건설 패널을 다시 열면 현재 선택 시설 기준 미리보기를 다시 계산하는 HUD 이벤트 동기화.
- [x] 잔액 경계와 건설 패널 닫힘을 검증하는 Playwright 회귀 2개 추가. 전체 67/67 통과.

## 9차 피드백 — 15레벨 기후 생존 캠페인·시간 경제

확정 주제: 지구온난화로 위기에 놓인 지구에서 살아남는 저탄소 미래 도시. 기존 5~6단계 일괄 진행과 과학 근거 입력/재설계 검증을 제거하고, 실제 도시 운영으로 통과하는 15개 퀘스트로 전환했다.

- [x] `QuestDefinitions`/`QuestSystem` 추가: 15개 레벨, 정확한 크레딧 보상, 시설 순차 해금, Lv.2/Lv.3 허가, 6×6 부지 확장, 폭염·야간·저탄소·물 절약 조건.
- [x] 현재 퀘스트 카드를 데스크톱/모바일에 항상 표시. 달성 순간 토스트·효과음·메뉴 알림점을 한 번만 띄우고, 보상은 사용자가 직접 수령.
- [x] 과학 근거 탭·입력·배지·진행 조건과 일괄 재설계 검증 버튼을 완전히 제거. 레벨 5/8/15의 2/3, 2/3, 3/4 퀴즈로 학습 확인을 대체하고 실패해도 도시 상태는 보존.
- [x] 실시간 5초=게임 1시간 운영. 중앙 모달·숨김 탭에서는 정지하며 오프라인 따라잡기 없음. 시간대별 태양광, 고정 순환 풍력, 4단계 월드 조명, 극한 폭염 수요를 적용.
- [x] 전력망: 거리 1칸 100%, 이후 칸당 6% 손실(최저 55%). 저장장치는 주변 좌우·상하·대각선 8칸에 95% 효율을 주는 중간 허브이며 용량/처리량과 저탄소·화석 저장량을 따로 추적. 필수→일반→절약 우선순위 적용.
- [x] 경제: 주거 세수, 공장/데이터센터 수입, 발전/저장/냉각 유지비, 노동인구·일자리 충원율, 과밀·오염 건강비·기후복구비를 시간당 정산. 0C 아래로 내려가지 않으며 퀘스트별 1회 긴급지원 제공.
- [x] 공장·주거지 도배 방지 패널티와 저탄소 이중 조건을 적용. 철거 환급은 누적 투자금의 `floor(50%)`로 확정하고 검사 모달에 투자/환급/손실을 동시에 표시.
- [x] 실제 정산 경로만 공유 `LineSegments` 한 개에 표시하고 5초마다 짧게 점멸. 에너지 입자와 연속 ambient 프레임은 없음.
- [x] 녹지의 상시 부유 객체를 제거. 녹지가 있을 때만 10~30초 무작위 간격으로 공유 새 풀 2~3마리가 약 2초 방문하고, 모달/숨김 탭에서는 정지.
- [x] 과학 근거 없는 운영 성적표로 교체: 퀘스트·퀴즈·평균 수익·송전 효율·저탄소 비율·고용·산업 충원·정전·과밀·건강 비용을 출력.
- [x] 라이트 모드 퀘스트 카드 대비, 보상 시설 한글명, 모바일 5버튼 하단바·44px 터치 타깃, 건설 시트와 퀘스트 카드 동시 노출을 시각 검수.
- [x] 저장 v2와 v1 마이그레이션: 기존 도시/크레딧을 보존하고 퀘스트·우선순위·배터리 상태를 추가하며 삭제된 근거 데이터는 폐기.

### 최종 검증 (2026-08-30, 15레벨 캠페인)

- `npm test -- --reporter=line --workers=1 --retries=0`: **107/107 통과** (3.6분, 재시도 없음).
- 시각 회귀 13개를 현재 UI로 전부 재생성한 뒤 다시 실행해 **13/13 통과**. 라이트/다크, 데스크톱/모바일, 건설/상태/메뉴, 성취, 위기, 진단, 카메라 회전을 포함.
- `npm run build`: 성공. JS 964.91KB(gzip 278.65KB), CSS 44.92KB(gzip 10.00KB). Three.js 중심의 기존 Vite 500KB 청크 경고만 유지.
- 대표 6×6 도시를 20시간 연속 결정 정산: draw call **20→20**, geometry **31→31**, resource revision **2→2**, 최종 전력선 segment 20, 콘솔 오류 0.
- 정적 검사: `git diff --check` 통과, 제품 코드에서 과학 근거/일괄 검증/옛 단계 진행 이벤트 참조 0.
- `frontend-design` 지침을 적용해 기존 시안의 청록·녹색·호박색 상태 언어는 유지하고, 퀘스트·전력 우선순위·철거 손실을 하나의 기후 관제 덱처럼 정리했다.
- 사용자가 소유하지 않은 저장소라는 지시에 따라 **git add/commit/push, PR, 배포를 전혀 수행하지 않고 로컬 변경만 보존**.

## 알아둘 것 (gotcha)

- 뇌 에너지(20W)는 출처 일치, AI 쪽 수치는 출처마다 10배 차이 — 게임 안에서 "출처 확인" 메시지로 활용 (수치 그대로 단정하지 않음). 출처는 `docs/gameplan.md`의 `### 보관 — 뇌-AI 에너지 비교 출처` 참고(2026-09-02에 옛 "3단계 섹션"에서 옮김). 이 비교 자체는 끝내 구현되지 않았다.
- "진짜 시장" 명칭은 게임에서 쓰지 않음 — 실제 교실 투표와 이름이 겹치면 혼란. 최종 등급 최상위 타이틀은 "그린시티 마스터".
- 공개 진행은 15개 퀘스트다. `STAGES` 숫자는 진단 장면과 편집 가능 여부를 연결하기 위한 내부 호환 값일 뿐 HUD에 노출하지 않는다.
- lucide 아이콘은 전체 세트가 아니라 실제 사용하는 27개만 `src/ui/Modal.js`의 `ICONS` 맵에 등록(트리쉐이킹). 새 아이콘을 `data-lucide="..."`로 추가하면 **PascalCase 키**로 이 맵에도 추가해야 함(`circle-help`가 아니라 `CircleHelp`) — kebab-case로 넣으면 에러 없이 조용히 안 보인다.
- 번들 크기가 약 941KB(gzip 270KB)로 Vite 경고 임계값(500KB)을 넘음 — 대부분 three.js/GLTFLoader. City Kit GLB는 JS 번들 밖의 정적 파일이다. 실사용(학교 1회 로딩)에는 문제없다고 판단해 코드 스플리팅은 하지 않음. 필요해지면 그때 처리.
- **보드는 이제 DOM이 아니라 Three.js 캔버스 하나다.** `#cityGrid`에는 `.city-cell`/`.diagnosis-cell` 버튼이 더 이상 없다 — 대신 `window.__clickCell(index)`(칸 클릭 시뮬레이션)와 `window.__getCellVisual(index)`(현재 시각 상태 읽기: selected/previewGood/previewBad/diagnosisState 등)를 테스트 훅으로 노출한다. 테스트는 이 훅을 쓰고, 실제 레이캐스팅 자체는 `game.spec.js`의 좌표 클릭 테스트 한 개로만 검증한다(카메라 각도가 바뀌면 좌표 테스트만 다시 손보면 됨).
- **정리 후보(기능엔 영향 없음)**: `style.css`의 `.city-cell`/일부 옛 진단 DOM 규칙은 3D 보드 전환 뒤 더 이상 사용되지 않는다. 이번 범위에서는 관련 없는 스타일 삭제로 회귀를 만들지 않기 위해 유지했다.
- 이 세션에서 시각 검수용으로 연 로컬 Vite 서버는 종료했다.
- 다음 세션에서 이어갈 것: 밸런스 실플레이 조정이나 퀴즈 문항 확장은 가능하지만, 배포/수익화/Git 원격 작업은 이 저장소에서 허가받지 않았다.

## 10차 피드백 — 2040 온보딩·연구·CC0 에셋·육각 도시·안전한 철거·주야간

- [x] 첫 접속 3장 스토리와 행동형 튜토리얼을 추가. 새 게임은 `2040-01-01 08:00`, 10C, 반경 2의 19칸 육각 보드로 시작한다.
- [x] 0/1/2/4배 시간 제어와 시간당 정산, 정확히 15개 퀘스트, 퀴즈 관문, 시설·레벨·연구 해금을 하나의 캠페인으로 연결했다.
- [x] 데이터센터 연구 5종과 2E 추가 수요, 90% 전력 조건, 14~30일 연구 기간, 취소 환급, 담당 시설 재지정을 구현했다.
- [x] 건설 카드는 비용·시간당 수입/유지비·발전/수요·탄소·물·노동을 표시한다. 모바일은 칸 선택 후 별도 확인이 필요하고, 데스크톱은 재사용 GLB 고스트를 마우스 오버 위치에 표시한다.
- [x] 건설 패널이 닫히면 건설 입력과 이득/갈등 표시·고스트가 함께 꺼진다. 돈이 부족하거나 잠긴 시설은 실제 비활성화된다.
- [x] 시설 철거는 즉시 실행하지 않고 `되돌릴 수 없습니다` 경고 모달에서 총투자·50% 환급·영구 손실을 확인한 뒤 실행한다. 확인 모달은 시간 정산을 멈추지 않는다.
- [x] 낮 하늘을 흰색 대신 하늘색 그라데이션으로 교체하고 이동하는 원형 태양을 추가했다. 08~16시는 같은 밝기, 06~08시와 16~18시는 따뜻한 색에서 하늘색/하늘색에서 따뜻한 색으로 전환한다.
- [x] 밤은 월드를 식별 가능한 최소 밝기로 유지하면서 달과 공유 `InstancedMesh` 창문 불빛을 표시한다. 주야간 변경은 시간 경계에서만 갱신하고 지속 애니메이션을 추가하지 않는다.
- [x] Kenney 공식 CC0 8개 팩에서 35개 대표 GLB를 선별·최적화했다. 도로/주거/상업/산업/자연/차량/사람/Hexagon Kit을 런타임 폴더와 레지스트리로 정리하고 원본 ZIP·라이선스·출처 기록을 보존했다.
- [x] 여러 primitive로 된 나무·바위·차량은 첫 파트만 버리지 않고 하나의 geometry로 병합해 기존 인스턴싱/드로우콜 예산을 유지한다.
- [x] Quaternius 공식 페이지에서 자동 다운로드 대상으로 확정할 수 없었던 에너지 팩 2종은 임의 파일로 대체하지 않았다. 수동 다운로드 항목과 절차형 태양광/풍력 폴백을 `assets-source/MANUAL_DOWNLOADS.md`에 기록했다.
- [x] AI 보좌관·자동 건설·과학 근거·배지/성취 시스템과 관련 제품 코드를 제거하고 퀘스트·운영 가이드·최종 운영 성적표로 통합했다.

### 최종 검증 (2026-08-30)

- `npm test -- --reporter=line --retries=0`: **172/172 통과** (2 workers, 3.7분, 재시도 없음).
- `npm run build`: 성공. HTML 9.30KB(gzip 2.87KB), CSS 52.93KB(gzip 11.48KB), JS 1,021.71KB(gzip 295.68KB). Three.js 중심의 500KB 청크 경고만 남는다.
- `npm run audit:assets`: **35 GLB**, public **872,492 bytes**, critical **88,504 bytes**. 예산(public 12MB / critical 3MB) 이하이며 카메라·라이트·애니메이션 오류 0.
- 성능: WebGL context 1개, 대표 37칸 도시 24 draw calls 이하, idle 연속 렌더 0, 30회 미리보기의 WebGL buffer create/delete 0/0, 전력선은 5초당 한 번만 점멸.
- 시각 회귀 16/16 통과. 하늘색 낮/노을/달·창문 불빛 밤, 라이트/다크, 건설·상태·설정, 퀘스트 완료, 진단, 카메라 회전, 모바일 기본/건설 시트를 직접 확인했다.
- 정적 검사: `git diff --check` 통과. 제거 대상 제품 코드 검색 결과는 저장 마이그레이션에서 옛 필드를 폐기하는 키 목록만 남는다.
- 저장소 안전: 사용자가 소유하지 않은 저장소라는 지시를 지켜 **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 전혀 수행하지 않았다**.

## 11차 피드백 — 사용자 제공 Quaternius 에너지 팩 마무리

원본 요청: `assets/`에 넣어둔 두 패키지가 올바른지 확인하고, 필요한 파일만 옮겨 폴더 구조와 패키지를 마무리.

- [x] `/Users/fecu/game-creator/assets/ultimate-space-kit`은 공식 설명과 같은 92개 모델·glTF 포함 구성, `Farm-Buildings-Pack`은 공식 설명과 같은 13개 모델·OBJ/FBX/Blend 구성임을 확인했다. 두 동봉 라이선스 모두 CC0 1.0이다.
- [x] Space Kit 동봉 `License.txt`의 제목이 “Ultimate Platformer Pack”으로 잘못 적힌 점은 숨기지 않고 라이선스 장부에 기록했다. 본문 CC0와 공식 Ultimate Space Kit 페이지의 CC0·92모델·glTF 표기는 일치하며 원문은 수정하지 않았다.
- [x] 상위의 사용자 원본 154MB는 삭제/이동하지 않았다. 필요한 원본만 `assets-source/manual/`에 복사하고, 자동 다운로드 기록과 충돌하지 않는 `manual-acquisition.json` 및 작은 선별 ZIP 2개로 해시를 고정했다.
- [x] 태양광은 `SolarPanel_Ground.gltf`와 `SolarPanel_Structure.gltf`, 풍력은 모바일에 적합한 `Windmill.obj/.mtl`을 선별했다. 원본 파일별 SHA-256은 `selected.json`에 남긴다.
- [x] 태양광 2종은 단일 GLB로 묶었다. 풍력은 OBJ/MTL 색공간을 웹용으로 보정하고 재질을 8×4 팔레트 1개로 통합했으며, 작은 타일에서 뭉치는 원본 다엽 바퀴를 제거해 716 triangles 타워 본체 + 공용 3엽 InstancedMesh로 구성했다.
- [x] 런타임 경로를 `public/assets/buildings/energy/{solar-small,solar-large,wind-turbine}.glb`로 통일하고 `assetRegistry.js`의 절차형 에너지 항목을 실제 GLB로 교체했다. 모든 11개 시설이 폴백 0개로 로드된다.
- [x] 수동 다운로드 목록은 0개로 닫고 Quaternius 라이선스 원문을 `assets-source/licenses/`와 `public/assets/licenses/` 양쪽에 보존했다. 변환 중 생긴 외부 sidecar와 일회성 `obj2gltf` 의존성은 제거했다.
- [x] 개발용 glTF CLI가 끌고 오던 취약한 `sharp 0.34.5`를 호환되는 `0.35.4`로 override하고 실제 전체 최적화를 재실행했다. `npm audit`은 취약점 0개다.
- [x] 레벨 색상은 원본 atlas를 과하게 어둡게 만들지 않는 밝은 회백/하늘/살구 곱셈색으로 조정했고, 데스크톱·모바일 16개 시각 스냅샷을 현재 GLB 기준으로 갱신·검토했다.

### 최종 에셋/검증 결과 (2026-08-30)

- `npm run assets:select && npm run assets:optimize && npm run assets:audit`: 성공. **38 GLB**, public **1,022,368 bytes**, critical **104,608 bytes**, 수동 다운로드 0개.
- 신규 에너지 런타임: 태양광 소형 **8,400B / 372 triangles**, 태양광 대형 **22,316B / 1,432 triangles**, 풍력 본체 **13,928B / 716 triangles**. 세 모델 모두 material 1개·texture 1개이며 외부 리소스/카메라/라이트/애니메이션이 없다.
- `npm test -- --reporter=line --workers=2 --retries=0`: **175/175 통과** (4.0분, 재시도 없음).
- 시각 회귀: **16/16 통과**. 실제 브라우저 근접 장면과 혼합 도시, 회전, 다크/라이트, 모바일을 직접 확인했고 콘솔 오류 0.
- 성능 회귀: **10/10 통과**. 37칸 대표 도시 24 draw calls 이하, idle 연속 렌더 0, 30회 redraw/HUD 전환의 GPU buffer create/delete 0/0.
- `npm run build`: 성공. HTML 9.30KB(gzip 2.87KB), CSS 52.93KB(gzip 11.48KB), JS 1,021.95KB(gzip 295.71KB). 기존 Three.js 중심의 500KB 청크 경고만 유지.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 전혀 수행하지 않았다**.

## 12차 피드백 — 연속 시간·고정 섬·병렬 연구·탄소 게임오버 마무리

- [x] 정산 주기를 5초에서 1초로 변경하고 화면을 가리던 정산 로딩 바를 제거했다. 1x는 실제 1초당 게임 1시간이며 2x/4x도 같은 규칙으로 가속한다.
- [x] 경제·전력·퀘스트는 정수 시간 경계에서 한 번만 정산하되, 날짜·시각과 태양/달의 CSS 위치는 `requestAnimationFrame` 보간으로 연속 표시한다. 시간 배율·모달 일시정지 순간에는 DOM 시계를 즉시 동기화하며 이 보간은 WebGL 프레임을 추가하지 않는다.
- [x] 최대 확장 부지 37칸을 시작부터 지형에 예약하고, 해안 24칸·수면 156칸·바다 평면으로 둘러싼 고정 섬을 구성했다. 퀘스트 확장은 기존 칸 위치를 바꾸지 않고 19칸에서 37칸을 개방한다.
- [x] 2040 해수면 상승과 남은 섬을 온보딩·도움말에 연결하고, 최대 부지와 1초 정산 규칙을 명시했다.
- [x] 연구를 데이터센터별 작업으로 재설계했다. 각 센터는 서로 다른 연구 하나를 독립 실행하고 2E를 추가 소비하며, 철거 시 해당 작업만 미배정 상태로 보존한다.
- [x] 연구 기간/비용을 태양광·풍력 7일/10C, 고효율 전지 10일/15C, 조력 10일/18C, 재생에너지 Lv.3 14일/24C로 조정했다. 퀴즈 정답은 진행 중인 모든 연구를 24시간 단축하고, 연구가 없으면 24시간을 한 번 적립한다.
- [x] Lv.2 허가를 퀘스트 7 보상으로 앞당기고, 완료한 태양광 연구가 실제 태양광 업그레이드와 퀘스트 8 판정에 적용되는 회귀를 고정했다.
- [x] 퀘스트 5 완료 뒤 탄소가 8을 초과하면 위험 시간이 누적되고 안전 운영 시 시간당 2시간 회복한다. 24/72/144시간 경고 후 168시간에 도시를 멈추는 닫기 불가 게임오버 모달을 추가했다.
- [x] 크레딧 계산·저장·표시를 소수 둘째 자리로 통일하고 `-0.00`을 방지했다. 저장 포맷은 병렬 연구·가속 적립·탄소 위기를 포함하는 v5이며 v1~v4 마이그레이션을 유지한다.
- [x] 발전 시설 위의 떠 있는 연결 아이콘과 레벨 받침을 제거했다. 전력은 공유 선 한 레이어가 5초마다 짧게 점멸하고, 사람/차는 정적 풀, 새는 녹지에서 10~30초 무작위 방문만 사용한다.
- [x] 잠금 시설은 클릭/마우스 오버 시 필요한 퀘스트·연구를 정확히 설명하고, 잔액 부족 카드도 필요한 금액을 알린다. 퀘스트 6은 스캐너 토글·다음 위험 화살표·행동 버튼으로 진행 대상을 직접 표시한다.
- [x] Kenney 공장 GLB의 검은 삼각 파편은 512px 팔레트 상단 검정 영역과 압축 노멀의 조합으로 확인했다. 시설 런타임은 실제 GLB geometry를 유지하면서 레벨 단색 재질과 재계산한 노멀을 사용해 회전 시에도 깨끗하게 보이도록 했다.

### 최종 검증 (2026-08-30, 연속 섬 운영)

- `npm test -- --reporter=line --workers=2 --retries=0`: **198/198 통과** (3.8분, 재시도 없음).
- 시각 회귀: **16/16 통과**. 낮·해질녘·밤, 달·창문 불빛, 다크/라이트, 카메라 회전, 진단, 데스크톱/모바일 건설 시트를 현재 섬과 GLB 기준으로 직접 확인했다.
- `npm run audit:assets`: **38 GLB**, public **1,022,368 bytes**, critical **104,608 bytes**, 폴백/수동 다운로드 0개.
- `npm run build`: 성공. HTML 9.44KB(gzip 2.91KB), CSS 52.95KB(gzip 11.51KB), JS 1,032.47KB(gzip 298.96KB). Three.js 중심의 500KB 청크 경고만 유지.
- `npm audit --audit-level=high`: 취약점 **0개**.
- 성능 계약: WebGL context 1개, 대표 37칸 도시 24 draw calls 이하, idle 연속 렌더 0, 30회 미리보기/HUD 전환의 WebGL buffer create/delete 0/0.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 13차 피드백 — 태양·달 화면 오브젝트 제거

- [x] 도시 장면 위에 겹치던 `.city-celestial-layer`와 원형 태양·달, 광륜, 위치 계산, 렌더러 상태 필드를 완전히 제거했다.
- [x] 시간대 표현은 하늘 그라데이션과 Three.js 월드 조명으로만 유지한다. 밤에는 기존 공유 창문 불빛을 남겨 시설을 식별할 수 있다.
- [x] 도움말의 태양·달 이동 문구를 `시계와 하늘 색은 부드럽게 변합니다`로 교체했다.
- [x] 천체 레이어가 다시 생성되지 않는 브라우저 회귀를 추가하고, 낮·노을·밤을 포함한 시각 기준 16개를 천체 없는 화면으로 갱신·직접 확인했다.
- `npm test -- --reporter=line --workers=2 --retries=0`: **198/198 통과** (4.1분, 재시도 없음).
- 저장소 안전: **Git 커밋·푸시·PR·배포를 수행하지 않고 로컬 변경만 보존**.

## 14차 피드백 — 시설 색상·맞물린 섬·Kenney 해안 경관

- [x] 모든 시설이 레벨 공통 회백색을 쓰던 경로를 종류별 11색 팔레트로 교체했다. 각 종류의 Lv.1/2/3은 서로 다른 명도·채도와 `0.84 / 1.00 / 1.18` 크기로 구분된다.
- [x] 실제 렌더된 `InstancedMesh`의 색상과 행렬 스케일을 읽는 회귀를 추가해 종류색 11개와 레벨별 색상·크기 차이를 고정했다.
- [x] Hexagon Kit GLB의 긴 지름과 높이 제한을 혼동하던 정규화를 수정했다. 건설 타일은 100.6%, 육지 100.6%, 해안 101.2%, 바다는 103% 커버리지로 맞물려 배경이 새는 틈을 없앴다.
- [x] 공식 Kenney Hexagon Kit ZIP에서 `building-dock`, `grass-hill`, `stone-hill`, `grass-forest`, `water-rocks`, `water-island`, `unit-ship` 7종만 추가 선별했다. 출처·CC0 원문·SHA-256·실사용 목록을 기존 파이프라인과 라이선스 장부에 보존했다.
- [x] 해안 반경에는 부두 2·초원 언덕 4·바위 언덕 2·숲 2개, 근해에는 암초 5·작은 섬 3·선박 2개를 고정 좌표로 배치했다. 특수 타일 좌표는 기본 흙/물 타일에서 제외해 겹침 깜박임을 막았다.
- [x] 해안 타일 4종은 동일 Kenney 팔레트를 공유하는 정적 geometry 하나로 병합하고, 근해 암초·섬·선박은 반복 모델별 InstancedMesh로 유지했다. 대표 37칸 도시의 24 draw-call 성능 계약을 그대로 지킨다.
- [x] `npm run assets:select && npm run assets:optimize && npm run assets:audit`: **45 GLB**, public **1,179,828 bytes**, critical **104,608 bytes**, 카메라·라이트·애니메이션 오류 0.
- [x] 집중 회귀 **37/37**, 갱신·검수한 데스크톱/라이트/주야간/모바일 시각 회귀 **16/16** 통과.
- [x] 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 수행하지 않고 로컬 변경만 보존**.

## 15차 피드백 — 중앙 경고·적자 건설 보호·고정 조명·아이콘 HUD

- [x] 중요 경고/퀘스트 알림 토스트를 오른쪽 아래에서 화면 중앙의 고대비 카드로 이동했다. 일반 안내 토스트는 기존 오른쪽 아래 위치를 유지한다.
- [x] 빈 칸에 시설을 놓기 전 현재 도시와 배치 후 도시를 같은 전력망·인력·운영비 규칙으로 각각 예측한다. 배치로 시간당 순수익이 더 악화되어 음수가 되면 현재/예상 순수익과 총수입·운영비·기타 부담을 비교하는 중앙 확인 모달을 띄우며, 사용자가 `그래도 건설`을 눌러야 실제 비용이 차감된다.
- [x] 상단에는 `YYYY-MM-DD` 일자만 표시한다. 1초당 게임 1시간 정산과 내부 시각(태양광 발전·야간 퀘스트·연구·탄소)은 그대로 유지한다.
- [x] 자동 주야간 렌더링을 제거하고 설정에 `낮 / 노을 / 밤` 고정 조명을 추가했다. 선택은 `localStorage`에 저장되며 정산·시간 가속 이후에도 바뀌지 않는다.
- [x] 상단과 건설 상세 지표를 `크레딧 → 전력 → CO₂ → 물 → 인력` 순서로 통일했다. 글자 라벨은 Lucide/기호 아이콘으로 교체하고 접근성용 `aria-label`과 툴팁은 유지했다.
- [x] 데스크톱·모바일·설정·건설 패널·낮 조명 시각 기준을 새 UI로 갱신하고 직접 확인했다.

### 최종 검증 (2026-08-30, 중앙 경고·고정 조명)

- `npm test -- --reporter=line --workers=1 --retries=0`: **216/216 통과** (6.8분, 재시도 없음).
- `npm run build`: 성공. HTML 10.40KB(gzip 2.99KB), CSS 57.58KB(gzip 12.25KB), JS 1,042.74KB(gzip 302.16KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- 시각 회귀 **16/16 통과**. 중앙 적자 모달, 데스크톱 건설 지표, 모바일 5개 자원 HUD, 낮/노을/밤 설정을 별도 브라우저 캡처로 확인했다.
- 성능 계약은 전체 회귀에서 유지됐다: WebGL context 1개, 대표 37칸 도시 24 draw calls 이하, idle 연속 렌더 0, 30회 미리보기/HUD 전환 GPU buffer create/delete 0/0.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 16차 피드백 — 해안 모래 타일 물 받침

- [x] 낮은 Kenney 모래/흙 타일이 단독으로 놓이던 구조를 수정해 해안 24칸 전체에 물 타일을 한 층 아래 받침으로 배치했다.
- [x] 모래와 해안 장식 타일을 바깥 바다의 수면 높이에 맞추고, 물 받침은 타일 한 장 두께 아래에 배치해 해안 가장자리가 꺼져 보이지 않게 했다.
- [x] 해안 받침 24개와 기존 바다 타일을 하나의 `InstancedMesh`로 유지해 추가 드로우콜 없이 렌더링한다.
- [x] 해안 받침 개수·전체 물 타일 개수·레이어 높이를 검증하는 회귀 테스트를 추가했다.
- [x] 관련 에셋·섬·성능 회귀 **18/18**, 해안이 보이는 데스크톱 시각 회귀 **2/2**, 프로덕션 빌드와 `git diff --check`가 통과했다.
- [x] 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 수행하지 않고 로컬 변경만 보존**.

## 17차 피드백 — 해안 부두 방향 조정

- [x] 두 `building-dock`의 기존 방사형 해안 정렬에 시계방향 120° 오프셋을 추가했다.
- [x] 회전 오프셋은 `Constants.js`에 두고 부두 레이어에만 적용해 언덕·숲·근해 소품 방향은 유지했다.
- [x] 실제 병합 geometry에 적용되는 두 부두의 최종 회전각을 브라우저 회귀로 검증한다.
- [x] 해안·에셋·성능 회귀 **18/18**, 전체 시각 회귀 **16/16**, 프로덕션 빌드가 통과했다.
- [x] 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 수행하지 않고 로컬 변경만 보존**.

## 18차 피드백 — 원본 시설 팔레트 복원·공장 교체·도시 방향 변화

- [x] 시설 GLB의 원본 텍스처를 제거하고 단색으로 덮던 런타임 경로를 없앴다. 원본 Kenney/Quaternius 팔레트 위에 시설 종류·레벨별 밝은 곱셈색을 적용해 모델 디테일과 레벨 구분을 함께 유지한다.
- [x] 공장은 공식 Kenney City Kit Industrial의 `building-a.glb` 대신 굴뚝·배관 실루엣이 분명한 `building-b.glb`를 같은 선별·최적화·라이선스 파이프라인으로 교체했다. 화력발전 굴뚝 모델과도 형태가 겹치지 않는다.
- [x] 주거·공장·데이터센터·원전·녹지는 셀 인덱스 기반의 결정적 60° 회전을 사용해 반복 배치가 덜 단조롭다. 방향성이 중요한 태양광·풍력·저장·냉각·조력·화력은 기존 방향을 유지한다.
- [x] 녹지 대표 모델은 텍스처가 없는 단일 나무 대신 이미 선별된 CC0 Hexagon Kit `grass-forest.glb`로 연결해 육각 셀 형태와 원본 팔레트를 유지한다.
- [x] 모든 시설은 종류별 `InstancedMesh` 한 레이어를 재사용한다. 레벨별 별도 geometry/draw layer를 추가하지 않아 대표 37칸 도시의 **24 draw calls** 한도를 유지한다.
- [x] `npm test -- --workers=1 --retries=0`: **217/217 통과** (6.9분, 재시도 없음). 에셋·회전·원본 텍스처·24 draw-call·GPU 버퍼 재사용·데스크톱/모바일 시각 회귀를 포함한다.
- [x] `npm run assets:audit`: **45 GLB**, public **1,185,420 bytes**, critical **110,200 bytes**. `npm run build`도 성공했으며 기존 Three.js 중심 500KB 청크 경고만 남는다.
- [x] 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 수행하지 않고 로컬 변경만 보존**.

## 19차 피드백 — 환경 퀘스트·간결 UI·건설 최종 확인

- [x] 6단계의 위험 스캐너 퀘스트를 `도시 물순환`으로 교체했다. 서로 인접한 데이터센터와 순환냉각이 각각 90% 이상 전력을 공급받고, 도시 물 사용량이 기준선 이하인 상태를 2시간 유지하면 완료된다.
- [x] 7단계는 `첫 태양광 전환`으로 재구성했다. 실제 태양광 전력 공급과 저탄소 전력 비율 30% 이상을 2시간 유지해야 하며, 6단계 보상 태양광과 7단계 보상 저장장치의 진행 흐름을 연결했다.
- [x] 기존 저장이 6단계 진단 화면에서 멈춘 경우 자동으로 편집 가능한 재설계 단계에 복구한다. 진단 UI·토글·힌트·퀘스트 행동 버튼과 관련 피드백은 실제 게임 흐름에서 제거했다.
- [x] 퀘스트 패널의 별도 타이틀 헤더를 없애고 `LEVEL · 퀘스트명 · 목표`를 한 줄로 배치했다. 진행률·보상·수령 버튼을 포함한 전체 높이를 데스크톱과 모바일 모두 줄였다.
- [x] 상단 시간 조작을 재생/정지 한 버튼과 `4×` 토글 한 버튼으로 정리했다. 일시정지 전 배속을 기억하며, 누락됐던 Lucide Play/Pause 등록도 브라우저 시각 검수 중 찾아 수정했다.
- [x] 데스크톱과 모바일 건설을 같은 2단계 흐름으로 통일했다. 빈 대지를 누르면 반투명 후보만 선택되고, 하단 `건설 확정`을 눌러야 비용 차감과 배치가 발생한다.
- [x] 건설 확정 바에는 배치 후 예상 시간당 크레딧, 전력 공급/수요, CO₂, 물 사용량과 현재 대비 증감을 표시한다. 기존 운영 적자 경고는 이 최종 확인 뒤 두 번째 안전 확인으로 유지한다.
- [x] 건설 확정 흐름에 맞춰 공용 플레이 헬퍼와 시설 검사·모션·퀘스트·시각 회귀를 갱신했다. 1초 시뮬레이션 틱 및 5초 전력선 점멸과 병렬 테스트가 경합하지 않도록 상태 읽기와 프레임 허용 기준도 실제 설계에 맞췄다.

### 최종 검증 (2026-08-30, 환경 퀘스트·건설 확인)

- `npm test -- --reporter=line --workers=2 --retries=0`: **223/223 통과** (4.8분, 재시도 없음). 데스크톱/모바일 시각 회귀 16개를 포함한다.
- `npm run audit:assets`: **45 GLB**, public **1,185,420 bytes**, critical **110,200 bytes**.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 수행하지 않고 로컬 변경만 보존**.

## 20차 피드백 — 행동형 퀘스트 알림·이동/고정 패널·연구 전력 경고·전력선 제거

- [x] 새 퀘스트/완료 조건 알림을 짧은 안내문 대신 `LEVEL / 퀘스트명 / 전체 목표 / 보상`을 담은 중앙 카드로 교체했다. 카드의 `퀘스트 열기` 또는 `보상 확인` 버튼은 해당 퀘스트 패널을 직접 연다.
- [x] 데스크톱 퀘스트 패널에 드래그 손잡이와 키보드 방향키 이동을 추가했다. 위치는 뷰포트 안으로 제한하고 `localStorage`에 저장해 새로고침 뒤에도 복원한다.
- [x] 핀 버튼으로 퀘스트 패널을 반투명 고정할 수 있다. 고정 중에는 건설 등 다른 HUD 패널과 동시에 보이며, 퀘스트만 닫아도 옆의 활성 패널은 유지한다.
- [x] 모바일에서는 기존 하단 시트 흐름을 유지하고 드래그/핀 조작을 숨겼다. 데스크톱 기본 위치는 상단 상태바와 겹치지 않게 조정했다.
- [x] 연구 전력 공급률이 90% 아래로 처음 떨어질 때만 중앙 경고를 한 번 띄운다. 담당 데이터센터에는 호박색 경고 링을 표시하고 연구 카드에는 `전력 부족 · 연구 일시정지 · 90% 필요`를 강조한다. 공급이 회복되면 링과 강조가 즉시 사라진다.
- [x] 복잡했던 전력 연결 `LineSegments`, 색상 버퍼, 정산 이벤트 구독, 5초 점멸/복원 타이머와 관련 GPU 상태를 모두 제거했다. 거리 손실·배터리 허브·공급 우선순위 같은 전력망 계산 규칙은 그대로 유지한다.
- [x] 시각 검수에서 퀘스트 패널의 신규 `position: relative`가 고정 위치를 덮어써 왼쪽이 잘리는 결함을 발견해 제거했다. 중앙 퀘스트 카드, 고정 패널+건설 팔레트, 데이터센터 경고 링, 전력 부족 연구 카드를 실제 브라우저로 다시 확인했고 콘솔 오류는 0개였다.

### 최종 검증 (2026-08-30, 퀘스트·연구 경고·전력선 제거)

- `npm test -- --workers=2 --retries=0 --reporter=line`: **226/226 통과** (4.8분, 재시도 없음). 데스크톱/모바일 시각 회귀 16개와 전력선 레이어/점멸 타이머 부재 계약을 포함한다.
- `npm run build`: 성공. HTML **11.32KB**(gzip 3.15KB), CSS **60.68KB**(gzip 12.81KB), JS **1,043.96KB**(gzip 302.79KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- `npm run audit:assets`: **45 GLB**, public **1,185,420 bytes**, critical **110,200 bytes**.
- `npm audit --audit-level=high`: 취약점 **0개**. `git diff --check`도 통과했다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 21차 피드백 — 배치 건설·시설 허가·안전한 퀘스트 창·저빈도 도시 모션

- [x] 건설을 한 칸 즉시 확인하는 방식에서 여러 빈 칸에 서로 다른 시설을 반투명 계획으로 올린 뒤 한 번에 확정하는 방식으로 변경했다. 같은 칸을 다른 시설로 교체하거나 같은 시설을 다시 눌러 계획에서 뺄 수 있고, 전체 비용·잔액·전력·CO₂·물 변화를 합산해 보여준다.
- [x] 일괄 확정은 원자적으로 처리한다. 돈·해금·부지·시설 수·화력 의존 조건 중 하나라도 어기면 크레딧과 도시를 전혀 바꾸지 않으며, 건설 창을 명시적으로 닫으면 미확정 계획과 고스트가 함께 사라진다. 저장 데이터에는 미확정 계획을 넣지 않는다.
- [x] 15개 퀘스트에 시설별 누적 최대 수를 배정했다. 기존 저장의 초과 건물은 보존하되 추가 건설만 막고, 건설 카드에 `현재 + 계획 / 최대`와 다음 증설 레벨을 표시한다. 핵발전이 있는 동안 마지막 화력발전은 철거할 수 없다.
- [x] 퀘스트 패널의 별도 드래그 버튼을 제거하고 창의 빈 영역 전체를 잡아 이동하도록 바꿨다. 상단 상태바와 우측 HUD 안쪽으로 자동 제한하며, 오래된 잘못된 저장 좌표는 무시한다. 핀·닫기 버튼은 헤더 안에 정렬했고 모바일은 드래그 없는 하단 시트를 유지한다.
- [x] 1,000 이상 자원은 K, 1,000,000 이상은 M으로 줄여 상·하단 HUD가 깨지지 않게 했다. 정확한 원래 값은 `title`과 접근성 라벨로 남긴다.
- [x] 공장·화력·원전은 짧은 연기/수증기, 데이터센터·주거·태양광·저장·냉각·조력은 작은 상태등, 풍력은 기존 공유 로터 회전으로 생활감을 추가했다. 녹지는 기존 10~30초 새 방문만 사용한다.
- [x] 시설 모션은 4~9초 간격, 한 번에 최대 3개, 0.6~1.6초만 작동하고 100ms 간격으로만 WebGL을 갱신한다. 연기·상태등은 각각 하나의 사전 할당 `InstancedMesh`를 공유하며, 모달 일시정지·탭 숨김·동작 줄이기 설정에서 즉시 정리된다.
- [x] 도움말에 배치 건설, 시설 수 허가, 핵발전의 화력 유지 규칙을 추가하고 `render_game_to_text()`에 화면의 미확정 건설 계획을 포함했다.

### 최종 검증 (2026-08-31, 배치 건설·시설 허가·저빈도 모션)

- 후속 운영·연구·퀘스트 개선까지 포함한 통합 Playwright **266/266 통과**로 교체 검증했다.
- 대표 37칸 도시 24 draw calls, idle 연속 렌더 중지, 미리보기/HUD 전환 GPU 버퍼 재사용 계약이 전체 회귀에서 유지됐다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 수행하지 않고 로컬 변경만 보존**.

## 22차 피드백 — 운영형 초반 퀘스트·연구 퀴즈·시설 콘솔·전체 퀘스트 보기

- [x] 1단계 보상으로 공장과 화력발전을 함께 해금하고, 2단계는 두 시설을 서로 인접하게 배치하면 즉시 완료되도록 바꿨다. 화력만 먼저 지어 운영비로 막히는 초반 구간을 제거했다.
- [x] 5단계를 핵발전 가동, 저탄소 전력 40% 이상, **CO₂ 12/h 이하**, 시간당 흑자 2시간 유지로 조정했다. 실제 전력망·운영비·19칸 육각 보드·시설 수 제한을 사용하는 달성 가능성 테스트로 15개 퀘스트 전체를 검증했다.
- [x] 간결한 퀘스트 창에는 `전체 내용 펼치기`를 추가했다. 펼치면 완료 조건과 보상 전체를 같은 창에서 확인하고 다시 간단히 접을 수 있으며, 모바일 하단 시트에서도 전체 내용이 화면 안에서 스크롤된다.
- [x] 퀘스트 보상 모달을 제거하고 완료 보상과 다음 퀘스트 목표를 중앙 알림 한 장으로 전달한다. 마지막 퀘스트만 최종 운영 성적표를 연다.
- [x] 데이터센터 상세창을 고정 헤더·운영/연구/관리 탭·스크롤 본문·고정 하단 액션 구조로 재설계했다. 다른 시설도 동일한 운영/관리 콘솔을 사용하고, 강화 조건·철거 손실은 관리 탭에서 확인한다.
- [x] 태양광·풍력·저장장치·조력·통합 재생전력 연구마다 에너지 퀴즈 4개씩, 총 20개를 배정했다. 문항과 보기 순서를 세션마다 섞되 정답 의미는 유지하고, 정답 하나당 해당 연구 시간의 25%만 단축해 네 문제를 모두 맞히면 연구를 끝낼 수 있다.
- [x] 데이터센터별 연구는 독립적으로 진행한다. 연구 가속 버튼은 그 연구의 퀴즈를 직접 열며 다른 데이터센터 연구나 미래 연구에 시간을 적립하지 않는다.
- [x] 도시 상태와 설정 창도 퀘스트 창처럼 빈 헤더 영역을 잡아 이동하고 위치를 저장한다. 모바일에서는 기존 하단 시트 동작을 유지한다.
- [x] 첫 사용자 입력 뒤 저음량 Web Audio 배경음이 실제로 시작되고 설정 버튼으로 모든 음원이 정리되는 흐름을 복구했다.
- [x] 마지막 화력발전 철거 제한을 일반 토스트가 아닌 최상위 차단 모달로 올렸다. 확인 후 기존 시설 관리 탭으로 돌아간다.
- [x] `CITY CONTROL` 장식 문구를 제거하고 연구 목록의 이름·시간·비용 간격을 다듬었다.

### 최종 검증 (2026-08-31, 운영·연구·퀘스트 UX)

- `npm test -- --reporter=line`: **266/266 통과** (8.6분). 퀘스트 달성 가능성, 연구별 20문항, 정답 셔플, 연구 독립성, BGM, 최상위 철거 제한, 이동 패널, 데스크톱/모바일 시각 회귀 17개를 포함한다.
- `npm run build`: 성공. HTML **11.95KB**(gzip 3.28KB), CSS **67.39KB**(gzip 13.84KB), JS **1,079.78KB**(gzip 314.97KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- `git diff --check`: 통과. Git 쓰기 작업과 원격 작업은 수행하지 않았다.
- 데이터센터 스크롤 콘솔과 모바일 퀘스트 전체 펼치기를 새 시각 기준으로 추가하고 직접 확인했다.

## 23차 피드백 — 인구 수용력·시설 인력·초반 녹지·산업 연기 강화

- [x] 주거지 레벨별 전체 인구를 10/15/22명으로 정의하고, 공장·발전소·데이터센터·저장·냉각·조력 시설에 레벨별 필요 인력을 배정했다. 녹지는 인력을 사용하지 않는다.
- [x] 여러 시설을 한꺼번에 올리는 건설 계획은 중간 순서가 아니라 최종 그리드의 `사용 인력 / 전체 인구`로 한 번만 검증한다. 주거지와 공장·발전소를 같은 계획에 넣어 함께 확정할 수 있고, 부족하면 정확한 부족 인원과 주거지 추가 안내를 표시한다.
- [x] 시설 강화로 필요 인력이 인구를 넘으면 강화를 막고, 주거지 철거로 도시가 인력 부족이 되면 최상위 철거 제한 모달을 띄운다. 이미 인력이 부족한 이전 저장은 보존하며 부족분을 악화시키는 행동만 막아 회복 플레이가 가능하다.
- [x] 상단 HUD, 건설 후 예상치, 건설 카드 상세, 시설 운영 콘솔을 `사용 인력 / 전체 인구` 표현으로 통일했다. 1,000 이상은 기존 K/M 축약을 사용하고 건설 예상치는 정수 인원으로 표시한다.
- [x] 퀘스트 2 완료 보상으로 녹지를 해금해 LEVEL 3부터 1칸 조성할 수 있게 했다. 녹지 허가는 퀘스트 6에서 2칸, 퀘스트 9에서 3칸으로 늘어나며 이후 기존 상한 증가를 이어간다.
- [x] 15개 퀘스트의 대표 도시를 시설별 허가 수, 인구 부족, 19칸 육각 보드, 실제 전력망, 운영 수익·탄소·물 조건으로 다시 검증해 전 단계가 달성 가능함을 확인했다.
- [x] 화력발전은 굴뚝 위에 큰 연기 6개, 공장과 원전은 각각 가벼운 연기/수증기 3개를 공유 풀에서 사용한다. 연기는 2.5~5초 간격의 제한된 배치에서만 작동하며 대표 도시의 24 draw-call 예산과 idle 렌더 중지 계약을 유지한다.
- [x] 데스크톱 1280×720과 모바일 390×844에서 주거지+공장 배치 계획을 직접 캡처했다. `4/10명` 예상치, 활성화된 일괄 확정 버튼, 화면 폭 내 5개 운영 지표와 콘솔 오류 0건을 확인했다.

### 최종 검증 (2026-08-31, 인구·녹지·산업 연기)

- `npm test -- --reporter=line --retries=0`: **279/279 통과** (8.7분, 재시도 없음). 인구 계산·건설/강화/철거 제한·15개 퀘스트 달성 가능성·연기 풀·데스크톱/모바일 시각 회귀를 포함한다.
- 대표 37칸 도시 **24 draw calls 이하**, idle 연속 렌더 중지, 미리보기/HUD 30회 전환 GPU buffer create/delete **0/0** 계약이 전체 회귀에서 유지됐다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 24차 피드백 — 단일 시설 콘솔·3열 아이콘 연구 목록

- [x] 주거지와 데이터센터를 포함한 모든 시설 상세창에서 `운영/연구/관리` 탭을 제거했다. 운영 지표·공간 규칙·전력 우선순위·투자/환급 내역을 한 스크롤 본문에 합치고, 강화와 철거는 고정 하단에 항상 노출한다.
- [x] 연구가 없는 일반 시설 창은 내용 높이만큼만 열리게 줄여 관리 탭을 없앤 뒤 남던 빈 공간을 제거했다. 연구가 있는 데이터센터만 긴 스크롤 콘솔을 유지한다.
- [x] 데이터센터 연구 목록을 별도 탭 없이 운영 정보 바로 아래에 배치했다. 태양광·풍력·저장장치·조력·통합 전력망은 각각 Lucide `Sun`, `Wind`, `BatteryCharging`, `Waves`, `Network` 아이콘으로 구분한다.
- [x] 연구 카드는 데스크톱과 390px 모바일 모두 한 줄에 3개를 유지하며, 아이콘·제목·비용·실시간만 표시한다. 기존 게임시간 표기와 긴 선행조건 문장은 카드 본문에서 제거했다.
- [x] 잠긴 연구 카드는 포커스 가능한 `aria-disabled` 상태로 유지한다. 마우스 hover/키보드 focus에서는 카드 안 툴팁으로, 클릭·터치에서는 중앙 알림으로 정확한 시설/기술/연구/크레딧 조건을 안내한다.
- [x] 연구 진행 스트립의 진행률, 전력 부족 강조, 퀴즈 가속, 취소·재배정과 데이터센터별 독립 연구 로직은 기존 동작을 유지한다.
- [x] 기능 변경을 먼저 실패시키는 회귀 테스트를 확인한 뒤 구현했으며, 데이터센터 데스크톱·모바일 시각 기준을 새 단일 콘솔에 맞게 갱신하고 직접 확인했다.

### 최종 검증 (2026-08-31, 시설 콘솔·연구 카드)

- `npm test -- --reporter=line --retries=0`: **283/283 통과** (9.0분, 재시도 없음). 단일 시설 콘솔, 강화·철거 즉시 노출, 독립 연구, 잠금 hover/touch, 데스크톱·모바일 3열과 시각 회귀를 포함한다.
- `npm run build`: 성공. HTML **12.16KB**(gzip 3.32KB), CSS **69.62KB**(gzip 14.26KB), JS **1,083.46KB**(gzip 316.14KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 25차 피드백 — 2단계 생산 운영 통합·3단계 첫 녹지

- [x] 2단계에 기존 3단계의 공장 운영 조건을 합쳤다. 공장과 화력발전을 육각형 한 칸 거리로 인접 배치하고, 공장 전력 공급률·가동률 50% 이상에서 실제 수입을 2시간 연속 발생시켜야 완료된다.
- [x] 한 시간만 흑자인 경우에는 완료되지 않고, 수입이 끊기면 연속 시간이 0으로 초기화된다. 공장과 화력발전이 떨어져 있으면 같은 운영 수치로도 완료되지 않는다.
- [x] 2단계 보상으로 녹지가 해금되어 LEVEL 3 시작부터 건설할 수 있다. 3단계는 `첫 녹지 조성`으로 교체해 녹지 1칸을 건설하면 즉시 완료된다.
- [x] 3단계의 기존 보상인 **6.00 💰 + 데이터센터 해금**은 유지했다. 저장 호환성을 위해 내부 퀘스트 ID는 유지하고 사용자에게 보이는 제목·목표·상세 설명만 교체했다.
- [x] 건설 목록을 `주거지 → 녹지 → 공장 → 화력발전 → 데이터센터 → …` 순서로 명시했다. 해금된 시설 우선 원칙을 유지하면서 LEVEL 3에서는 녹지가 주거지 바로 뒤에 나타난다.
- [x] 퀘스트 진행률은 2단계에서 2시간 누적, 3단계에서 녹지 개수 기반으로 각각 계산한다. 1~7단계와 8~15단계의 실제 전력·경제·인력·시설 허가 기반 달성 가능성도 다시 검증했다.
- [x] 데스크톱과 390px 모바일 건설창의 시각 기준을 새 순서로 갱신하고 녹지 카드가 두 화면 모두 두 번째에 표시되는 것을 직접 확인했다.

### 최종 검증 (2026-08-31, 초반 퀘스트·녹지 순서)

- `npm test -- --reporter=line --retries=0`: **285/285 통과** (9.3분, 재시도 없음). 2단계 인접·흑자 누적/초기화, 3단계 녹지, 15단계 달성 가능성, 저장, 데스크톱·모바일 시각 회귀를 포함한다.
- `npm run build`: 성공. HTML **12.16KB**(gzip 3.32KB), CSS **69.62KB**(gzip 14.26KB), JS **1,083.71KB**(gzip 316.24KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 26차 개선 — 시스템 감사 후속: 단일 운영 규칙·성장·초기화·평가

- [x] 시설 레벨의 공급·수요·수입·유지비·탄소·물 배율을 `FacilityOperationSystem` 한 곳으로 모았다. 전력망, 시간당 경제 정산, 정적 미리보기, 시설 상세가 같은 레벨 값을 사용한다.
- [x] 녹지는 실제 시간당 CO₂를 낮추고 도시 합계는 0 아래로 내려가지 않는다. 순환냉각은 도시 어디서나 물을 줄이던 효과를 제거하고, 인접한 데이터센터와 핵발전의 물 부담만 줄인다.
- [x] 도시 차트와 최종 성적표는 마지막 실제 정산의 전력 공급률·CO₂·물 사용량을 우선 사용한다. 시설 상세도 인접 효과가 반영된 시설별 실시간 탄소·물을 표시한다.
- [x] `통합 재생전력망` 연구에 배터리 기술 Lv.3을 추가했다. 최대 레벨 3인 모든 시설이 허가·인력·기술 조건을 충족하면 실제로 Lv.3까지 강화되는지 회귀 테스트로 고정했다.
- [x] 일반 초기화와 탄소 게임오버 초기화를 하나의 경로로 통합했다. GameState, 실제 시뮬레이션 컨트롤러, 0×/1×/4× 배속, 누적 진행률, 모든 pause reason, 모달·토스트·퀘스트 축하·오디오 상태를 함께 초기화하고 `GAME_RESET`을 발행한다.
- [x] 최종 점수를 운영 50점, 설계 30점, 지식 20점으로 재구성했다. 저탄소·송전·정전·흑자·인력·사회 비용이 운영 점수에 직접 반영되고, 연구 퀴즈는 `passed`가 아니라 실제 정답률로 채점한다.
- [x] 퀘스트 9를 `저탄소 저장 허브`로 바꾸고 저장 경로를 3시간 연속 유지하며 8E를 누적하도록 문구·진행률·판정을 통일했다. 공급이 끊기면 시간과 에너지가 함께 초기화된다.
- [x] 퀘스트 14는 Lv.3 시설 하나 이상 또는 통합 재생전력망 연구 완료를 요구한다. 기존 저탄소 70%, 기준 미만 물 사용, 흑자 4시간 조건은 유지한다.
- [x] 초기 10크레딧에서 시작해 실제 건설비·연구비·시간당 수입·보상·철거 환급을 이어가는 1→15 연속 캠페인 테스트를 추가했다. 외부 크레딧 주입이나 완성 도시 fixture 없이 최종 퀴즈와 성적표까지 도달한다.
- [x] 변경 설계와 구현 계획을 `docs/superpowers/specs`와 `docs/superpowers/plans`에 기록했다. Git 이력은 변경하지 않았다.

### 최종 검증 (2026-08-31, 시스템 감사 후속)

- `npm test -- --reporter=line --retries=0`: **297/297 통과** (9.0분, 재시도 없음). 전체 데스크톱·모바일·시각·성능·콘솔 오류와 신규 연속 캠페인 테스트를 포함한다.
- `npm run build`: 성공. HTML **12.16KB**(gzip 3.32KB), CSS **69.62KB**(gzip 14.26KB), JS **1,087.39KB**(gzip 317.40KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- 최종 성적표를 1440×900 실제 브라우저로 캡처해 3개 점수 영역, 퀴즈 정답률, 운영 상세, 버튼 정렬과 배경 대비를 직접 확인했다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 브랜치·worktree·히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 27차 피드백 — CO₂ 안전선 완화·저장 허브 기반 탈화력

- [x] 탄소 위기와 탄소 회복 비용의 안전선을 시간당 CO₂ **8 → 10**으로 함께 올렸다. 화력 Lv.1과 공장 Lv.1이 모두 가동되는 최소 생산 도시가 추가 탄소 비용과 위기 누적 없이 운영된다.
- [x] 퀘스트 5의 전환 목표인 저탄소 전력 40% 이상, CO₂ 12 이하, 흑자 2시간은 유지한다. 도움말·경고·게임오버·퀘스트 설명의 장기 안전 기준은 모두 10으로 통일했다.
- [x] 퀘스트 9 `저탄소 저장 허브` 완료 전에는 핵발전에 화력발전 1기를 계속 요구한다. 완료 후에는 에너지저장 시설 1기 이상이 핵발전 예비력을 대신하므로 핵발전을 유지한 채 마지막 화력을 철거할 수 있다.
- [x] 탈화력 핵발전망에서 마지막 배터리를 철거하거나, 저장 허브를 완료했지만 배터리 없이 화력을 철거하는 우회는 차단한다. 직접 건설·일괄 건설 계획·철거가 같은 예비력 판정을 사용한다.
- [x] 핵발전 예비력 검사가 건설 비용 검사를 우회하지 않도록 자금 부족 경계 조건도 회귀 테스트로 고정했다.

### 최종 검증 (2026-08-31, 탄소 안전선·탈화력)

- `npx playwright test tests/e2e/unit --reporter=line --retries=0`: **173/173 통과**. 실제 초기 자금으로 진행하는 1→15 연속 캠페인을 포함한다.
- `npm test -- --reporter=line --retries=0`: **304/304 통과** (9.2분, 재시도 없음). 데스크톱·모바일·시각·성능·콘솔 오류 회귀를 포함한다.
- `npm run build`: 성공. HTML **12.16KB**(gzip 3.32KB), CSS **69.62KB**(gzip 14.26KB), JS **1,088.83KB**(gzip 317.82KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 브랜치·worktree·히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 28차 개선 — 선택형 기후 운영 캠페인·24시간 기상이변 대비·웹 BGM

- [x] `AI_CITY_GAMEPLAY_REDESIGN_SPEC.md`를 기준으로 6개 기초 퀘스트 뒤에 `전환 방향 선택(2/3) → 도시 전문화(2/3) → 회복탄력 도시(3/4)` 목표 세트를 연결했다. 퀴즈는 연구 가속과 최종 보너스로만 남기고 승리 조건에서 분리했다.
- [x] 동부/서부 9칸 1차 확장, 반대편 2차 확장, 지역 특성과 확장 유지비를 실제 건설비·시설 생산·운영비에 연결했다. 운영 모드, 시설 우선순위, 배터리 정책, 분기 연구, 기후 이벤트, 최종 5단계 스트레스 테스트와 5축 성적표를 하나의 캠페인으로 연결했다.
- [x] 기존 15단계 저장은 v6로 안전하게 정규화하고, 이전 `renewable3` 연구는 네 종류의 Lv.3 기술로 복원한다. Q9/Q14 진행 중 저장과 37칸 외곽 시설도 보존한다.
- [x] 초기 10크레딧에서 외부 자금·완료 상태 주입 없이 실제 HUD와 3D 캔버스를 조작해 동부 안정형 캠페인을 완주했다. 별도 단위 경로로 동부 안정형과 서부 분산형 모두 이벤트 2회와 최종 스트레스 테스트를 통과했다.
- [x] 안정형 실측은 운영점수 78.4, 프로필 `stable-energy`, 정전 0시간, 예비력 89.5%, 송전효율 98.7%, 평균 순수익 +0.52였고 스트레스 테스트 평균 순수익 +0.37, 최종 47.15크레딧으로 생존했다.
- [x] 분산형 실측은 운영점수 77.5, 프로필 `smart-grid`, 재생전력 15.2, 배터리 공급 비중 14.4%, 예비력 111.6%, 송전효율 95.2%, 운영 결정 3회였고 스트레스 테스트 평균 순수익 +0.11, 최종 14.99크레딧으로 생존했다.
- [x] 사람 플레이 목표를 15~30분으로 정의하고, CH.2 이후 대표 2분 구간마다 발전/저장, 연구 분기, 강화/확장, 시설 모드, 배터리 예비력, 이벤트 대응 등 3개의 의미 있는 결정을 갖도록 계약 테스트로 고정했다. 자동화된 실제 UI 완주는 4배속 기준 약 1분 12초였다.
- [x] 기상이변의 기존 6게임시간 예보는 1배속 6초·4배속 1.5초라 실질적인 대비가 불가능함을 확인했다. 모든 이벤트를 24게임시간 전에 예고하고 첫 예보 순간 게임을 자동 일시정지하며, 중앙 준비창을 닫아도 플레이어가 직접 재생하기 전까지 멈춤을 유지하도록 바꿨다.
- [x] 준비창은 이벤트별 예상 영향과 권장 대응을 보여준다. 연속 이벤트는 `이전 이벤트 종료 → 3시간 휴지기 → 24시간 새 예보 → 발동` 순서로 예약해 앞 이벤트 때문에 다음 대비 시간이 사라지지 않도록 수정했다. 상단 예보와 회복탄력 목표에도 24시간 준비 규칙을 표시한다.
- [x] 대표 37칸 도시와 지역·운영 모드·기후 이벤트가 동시에 활성화된 장면 모두 WebGL 1개와 24 draw calls 이하를 유지한다. 정착 장면은 연속 렌더를 중지하고, 미리보기 30회와 HUD 전환 30회에서 GPU buffer create/delete가 0/0이었다.
- [x] `public/assets/eco-city.mp3` 원본 4,119,934바이트(약 216kbps, 48kHz)를 96kbps/44.1kHz 스테레오 MP3 1,828,197바이트로 다시 인코딩해 전송량을 55.6% 줄였다. 재생 길이 약 152.29초는 유지했다.
- [x] 원본은 `assets-source/audio/eco-city-original.mp3`에 보존하고 변환 명령·크기·SHA-256을 `assets-source/audio/README.md`에 기록했다. 런타임은 음악을 JS 번들에 넣거나 선로딩하지 않고 첫 사용자 조작 후 `HTMLAudioElement`로 스트리밍·반복 재생한다. 효과음은 기존 Web Audio 경로를 유지한다.

### 최종 검증 (2026-09-01, 선택형 캠페인·기후 대비·웹 BGM)

- `npm test -- --reporter=line --retries=0`: **377/377 통과** (11.2분, 재시도 없음). 실제 HUD/캔버스 완주, 두 전략 캠페인, 24시간 예보·자동 정지, BGM 파일 크기/첫 조작 재생, 데스크톱·모바일 시각·성능 회귀를 포함한다.
- `npm run build`: 성공. HTML **12.65KB**(gzip 3.45KB), CSS **79.70KB**(gzip 15.79KB), JS **1,176.60KB**(gzip 345.19KB). Three.js 중심 500KB 청크 경고는 남지만 초기 기능·성능 계약은 통과했다.
- 빌드 결과 `dist/assets/eco-city.mp3`는 **1,828,197바이트**이고 공개 런타임 파일과 SHA-256 `244aac7f...f22ed`가 일치한다. 4.12MB 원본은 `dist`에 포함되지 않는다.
- `npm run audit:assets`: **45 GLB / 1,185,420바이트**, 초기 핵심 에셋 **110,200바이트**로 통과했다. `npm audit --audit-level=high`: **취약점 0건**. `git diff --check`: 통과.
- 앱은 현재 한 문서에서 한 번만 부팅하며 지원되는 SPA 재마운트 경로가 없다. 3D와 개별 컨트롤러의 내부 정리는 유지하고, 확인되지 않은 전역 재부팅 경로를 위한 앱 전체 disposer는 추가하지 않았다. 향후 SPA 재마운트를 도입하면 이 계약을 별도로 추가한다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 브랜치·worktree·히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 29차 개선 — Game Clock 건설·강화 프로젝트

- [x] 일괄 건설 확정은 시설을 즉시 완공하지 않고, 비용 차감·타일 점유와 함께 시설별 3~18게임시간의 독립 공사를 동시에 시작한다. 공사 현장은 완공 전까지 운영 효과·운영비·인력·인접 효과가 모두 0이다.
- [x] 강화는 Lv.1→2 8시간, Lv.2→3 15시간이며 기존 레벨을 유지한 채 제한 가동한다. 일반 시설 70%/50%, 주거 80%, 배터리 충방전 50%, 데이터센터 전력 70%·연구 50%·수입 60% 특례와 운영비·인력 100%를 정산 코어에 통합했다.
- [x] 공사는 단일 Game Clock으로 진행되어 일시정지에서는 멈추고 1×/4× 전체 시뮬레이션과 함께 가속된다. 완공 판정은 같은 시간의 전력·경제·탄소·물·연구·퀘스트 정산보다 먼저 실행되어 완공 틱부터 새 성능이 적용된다.
- [x] 진행률 25%/75% 경계의 80%/65%/50% 환급, 신규 현장 제거, 강화 취소 시 기존 레벨·운영모드 즉시 복원, 100% 이후 취소 금지를 구현했다.
- [x] 저장 형식을 v7로 올려 동시 건설·강화 진행도, 실제 결제액, 기존 운영모드, 배터리·연구 상태를 복구한다. v6 이하의 기존 완공 시설에는 가상의 공사를 만들지 않는다.
- [x] 실제 정산기와 같은 코어를 복제 상태에서 이벤트·저장·퀘스트 보상 없이 실행하는 순수 예측을 추가했다. 일괄 공사의 최대 남은 시간과 강화 중/완공 후 전력·수익·인력·CO₂·물·배터리 위험을 착공 전에 표시한다.
- [x] 3D 현장은 기초→골조→외형 단계로 보이고, 강화는 기존 모델과 비계를 함께 표시한다. 선택한 공사에는 DOM 진행 바·퍼센트·남은 게임시간·취소 버튼을 제공하며 WebGL은 단계 변경 때만 갱신한다.
- [x] 착공 시에는 완공 모션·시설 운영·퀘스트 배치 판정을 발생시키지 않고, 실제 완공 이벤트에서만 시설 진입 모션·완공 알림·오디오·퀘스트 판정이 실행된다.

### 최종 검증 (2026-09-01, Game Clock 공사)

- `npm test -- --reporter=line --retries=0`: **411/411 통과** (11.3분, 재시도 없음). 실제 HUD/3D 캔버스 캠페인 완주, 신규 공사·강화·취소·완공 경계·예측 일치·저장 v7·시각·모바일·성능 회귀를 포함한다.
- `npm run build`: 성공. HTML **12.90KB**(gzip 3.53KB), CSS **83.52KB**(gzip 16.35KB), JS **1,198.92KB**(gzip 351.84KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- `npm run assets:audit`: **45 GLB / 1,185,420바이트**, 초기 핵심 에셋 **110,200바이트**로 통과했다. `npm audit --audit-level=high`: **취약점 0건**. `git diff --check`: 통과.
- 대표 37칸 도시 **24 draw calls 이하**, idle 연속 렌더 중지, 미리보기/HUD 반복 조작 GPU buffer create/delete **0/0** 계약을 유지했다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 브랜치·worktree·히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 30차 개선 — 도시 상태 그래프 연속 보간

- [x] 시간당 운영값은 기존 Game Clock 정산값을 그대로 사용하고, 레이더 그래프의 표시만 다음 틱 직전까지 선형 보간한다.
- [x] 1×는 900ms, 2×는 450ms, 4×는 225ms로 현재 배속의 실제 정산 간격 90%를 사용해 그래프가 뒤처지지 않도록 했다.
- [x] 도시 상태 패널이 닫혔거나 일시정지 상태이거나 `prefers-reduced-motion`이 활성화되면 0ms 즉시 갱신한다. 닫힌 그래프는 백그라운드 애니메이션 프레임을 소비하지 않는다.
- [x] 실제 캔버스 픽셀을 비교해 열린 그래프는 600ms 이후에도 보간 중이고, 닫힌 그래프는 갱신 후 150ms 동안 추가 프레임이 없음을 브라우저 회귀 테스트로 고정했다.

### 최종 검증 (2026-09-01, 그래프 연속 보간)

- `npm test -- --reporter=line --retries=0`: **415/415 통과** (11.6분, 재시도 없음).
- `npm run build`: 성공. HTML **12.90KB**(gzip 3.52KB), CSS **83.52KB**(gzip 16.35KB), JS **1,199.39KB**(gzip 352.05KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- 대표 37칸 도시 **24 draw calls 이하**, HUD 30회 전환 GPU buffer create/delete **0/0**, `git diff --check` 통과.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 브랜치·worktree·히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 31차 개선 — Game Clock 기반 연속 공사 표시

- [x] 시간 정산은 기존 정수 Game Hour 틱을 유지하되, 현재 틱의 실시간 진행분을 공사 표시용 분수 시간으로 보간한다. 1×/4×에서 자연스럽게 진행하고 일시정지 즉시 멈춘다.
- [x] 선택 시설 하나만 표시하던 진행 HUD를 모든 동시 건설·강화 프로젝트의 DOM HUD 풀로 확장했다. 선택 시설은 단계·퍼센트까지, 나머지는 작은 진행 바로 보여준다.
- [x] 공사 표시 때문에 WebGL을 60fps로 계속 제출하지 않는다. DOM 진행 바만 갱신하고 3D 현장 geometry는 기존 단계 경계에서만 dirty 처리한다.

## 32차 수정 — 육각 전력 판정·퀘스트·공장 관리

- [x] 육각 거리 자체는 여섯 방향 모두 동일했지만, 같은 우선순위 소비지와 발전원을 셀 배열 번호로 정렬해 회전 방향에 따라 결과가 달라지던 원인을 제거했다. 소비 시설 역할 순서와 저탄소 우선 급전을 명시해 동일 도시를 60도 회전해도 데이터센터 공급률과 저탄소 비율이 같다.
- [x] 퀘스트 5는 실제 허가·전력망·경제 규칙에서 달성 가능한 공통 배치로 회귀 검증하고, 저탄소 우선·화력 예비력 원칙을 설명에 반영했다.
- [x] 건설 중 공장은 `현장 철거`, 완공 공장은 일반 비가역 `철거`로 구분했다. 짧은 데스크톱 화면에서도 본문만 스크롤되고 강화·철거 footer는 항상 보이도록 수정했다.
- [x] 시설 허가 최대치에 도달하면 가상 배치를 계획에 넣은 뒤 오류로 만드는 대신, 클릭 시점에 거절한다. 기존 계획은 유지하고 토스트로 현재 허가 한도를 안내한다.

## 33차 수정 — 건설 설명과 실제 정산 수치 통일

- [x] 건설 카드의 Lv.1 수치는 별도 원시 상수를 직접 조합하지 않고 실제 전력·경제·환경 정산이 사용하는 `effectiveFacilityStats`를 단일 기준으로 읽는다.
- [x] 주거지·공장·데이터센터의 크레딧은 보장 수입처럼 보이지 않도록 `시설 수지 상한`으로 표시한다. 전력·인력·취업률·오염·운영 모드가 반영된 실제 값은 완공 시설 상세에서 보여준다.
- [x] 완공 시설 상세는 `수입`만 표시하던 값을 `실제 수입 - 고정 운영비`인 시설 수지로 바꿨다. 같은 줄 아래에 환경·확장·다른 시설 운영비까지 합친 `도시 전체 순수익`을 별도로 표시해 주거지가 수입을 내면서도 도시 잔액이 감소할 수 있는 이유를 구분한다.
- [x] 소비 시설은 실제 `공급량/수요량 E · 공급률`, 발전 시설은 현재 게임 시각·풍황·연구·이벤트가 반영된 현재 발전량을 표시한다. 건설 계획 하단의 크레딧 지표는 `도시 순수익`으로 명시했다.
- [x] 순환냉각의 원시 `물 -5` 표기를 제거했다. 자체 물 사용은 0이고, 전력이 공급될 때 인접 데이터센터·핵발전의 물 사용을 줄이는 실제 규칙으로 설명과 수치를 맞췄다. 냉각 계수도 `COOLING_RULES` 한 곳으로 모았다.
- [x] 모든 시설 설명을 실제 운영 규칙 중심으로 다시 작성하고, 데스크톱 건설 패널의 기존 190px 높이 계약과 모바일 3열/공유 상세 레이아웃을 유지했다.

### 최종 검증 (2026-09-01, 공사 표시·육각 판정·시설 수치)

- `npm test`: **432/432 통과** (12.2분). 의도적으로 달라진 건설 패널 시각 기준을 직접 검토·갱신했고, 갱신 기준을 포함한 데스크톱·모바일·시각·성능·실제 캠페인 전체 회귀를 재시도 없이 통과했다.
- 공사 완공을 현실 5초 경계에 기대던 퀘스트 알림 테스트는 Game Clock으로 명시적으로 완공하도록 바꿨다. 단독 5회 반복 **5/5 통과** 후 전체 회귀에서도 통과했다.
- `npm run build`: 성공. HTML **12.92KB**(gzip 3.53KB), CSS **84.46KB**(gzip 16.49KB), JS **1,203.79KB**(gzip 353.59KB). 기존 Three.js 중심 500KB 청크 경고만 남는다.
- 저장소 안전: **git add/commit/push, PR, 배포, 원격 변경, 브랜치·worktree·히스토리 변경을 전혀 수행하지 않고 로컬 변경만 보존**.

## 34차 수정 — 주거 세금 표시와 실제 정산

- [x] 전력이 0이면 주거지의 기본 세금까지 0이 되던 공식을 수정했다. Lv.1 주거지는 정전·무고용 상태에서도 `+0.13 💰/h` 기본 세금을 정산하고, 전력과 고용이 개선되면 최대 `+0.50 💰/h`까지 늘어난다.
- [x] 건설 상세의 모호한 `시설 수지 상한`을 `주거 세금 +0.13~+0.50/h`로 바꾸고, 완공 시설 상세도 `주거 세금`과 `도시 전체 순수익`을 구분했다.
- [x] 관련 경제·건설 미리보기·데스크톱·모바일·시각 회귀 56개를 통과했다.

## 35차 수정 — 7단계 고정 표시와 최종 시험 전환

- [x] 6개 기초 퀘스트 후 선택 목표·최종 시험으로 진행해도 상단이 계속 `복구 단계 7 / 15`로 남던 레거시 HUD 분기를 제거했다.
- [x] HUD는 실제 진행 상태에 따라 `복구 퀘스트 1–6 / 6`, `운영 챕터 2 / 4`, `운영 챕터 3 / 4`, `최종 시험 준비·진행·재도전`, `도시 복구 완료`를 구분해 표시한다.
- [x] `회복탄력 도시` 목표 보상을 받으면 `stressTest=ready`와 함께 내부 `chapter=4`, `chapterChanged=true`가 적용되도록 수정했다.
- [x] 실제 10크레딧 시작에서 `기초 1–6 → 챕터 2 → 챕터 3 → 최종 시험 → 보고서`까지 이동하는 진행 회귀를 포함한 관련 14개 테스트를 통과했다.

## 36차 수정 — 건축 1건씩 짓기 + 카메라 리셋 버튼 제거

원본 요청: "지금 여러 건물을 한번에 짓는 식인데, 하나당 하나씩 짓도록 바꾸고 싶어. 건축에서 건물 클릭 -> 건축탭 내려감 -> 건축후 예상 탭 -> 건축 미리보기 위에 o, x 위젯 뜸 -> o 클릭시 건설, x 클릭시 건설취소 -> 건설탭 복귀 이런 식으로. city-camera-reset button은 제거해라."

- [x] 건설 확정 방식을 "여러 건물을 계획에 담아 한 번에 확정"에서 "한 번에 하나씩: 위치 클릭 → 건축후 예상 미리보기 → O/X로 확정·취소"로 바꿨다. 시스템 레벨(`ConstructionPlanSystem.js`)의 다중 항목 지원 자체는 그대로 두고, UI 레벨(`GridView.js`의 `handleSceneCellClick`)에서만 게이트를 걸었다 — 확정 이후에는 기존처럼 여러 건물이 동시에 공사 진행될 수 있다(선택 흐름만 바뀐 것이고 게임 규칙은 그대로).
- [x] O/X 확정 위젯은 처음엔 하단 고정 바에 넣었다가, 사용자 피드백으로 3D 보드 위 미리보기 유령 모형 바로 위에 뜨는 월드 앵커 오버레이로 바꿨다(`CityScene3D.js`의 `setBuildOxWidget`/`syncBuildOxWidget`, 기존 공사 진행 배지 `world-construction-progress`와 같은 화면 좌표 투영 기법을 재사용). 가장자리 칸에서 투영 좌표가 컨테이너 밖으로 나가 위젯이 화면 밖에 숨어버리는 문제가 있어 컨테이너 안쪽으로 좌표를 clamp했다. 하단 바는 시설명·비용·잔액·예상 지표·완공 타임라인만 보여주는 정보 패널로 남았다.
- [x] 건물을 클릭해 위치를 정하면 건축 독(facility dock)이 접히고 안내 문구가 뜨며, O 확정 또는 X 취소 후 독이 복귀한다(`DockView.js`).
- [x] 확정(O) 직후 배치 무장 상태를 해제해, 다음 건물을 지으려면 독에서 시설을 다시 골라야 한다 — 확정 후에도 커서를 올리면 미리보기가 계속 뜨고 클릭하면 바로 다음 건설이 시작되던 문제를 고쳤다(`facilityArmed` 상태, `GridView.js`).
- [x] `city-camera-reset` 버튼(3D 시점 초기화 아이콘)을 생성 코드·이벤트 바인딩·정리 코드·CSS·전용 테스트까지 전부 제거했다. 카메라 초기화 자체(`window.__resetCityCamera`, 보드 확장 시 자동 리셋)는 버튼과 무관하게 남아 있다.
- [x] O/X 버튼을 반투명 아웃라인에서 불투명 빨강/초록 원형 버튼으로 바꿔 3D 배경 위에서도 잘 보이게 했다.
- [x] 건설 위험 확인 모달·도움말 텍스트·퀘스트 힌트 문구 중 "여러 시설을 계획에 담아 한꺼번에 확정" 전제로 쓰여 있던 문구를 새 흐름(하나씩 확정)에 맞게 고쳤다.

### 최종 검증

- `npx playwright test`: 전체 회귀에서 이번 변경과 직접 관련된 파일(`build-preview`, `camera`, `construction-progress`, `perf`, `hud`, `game`, `visual`, `construction-plan` 단위 테스트 등)은 모두 통과했다. 다중 배치를 가정하던 기존 테스트·공용 헬퍼(`tests/helpers/playthrough.js`)도 새 흐름(칸마다 확정)에 맞게 갱신했다.
- 전체 회귀 중 이번 작업과 무관한 기존 실패 10건을 발견했다 — 게임 시각 단위를 시(hour)에서 일(day) 기준으로 옮기는 리팩터링이 이미 별도로 진행 중인 것으로 보이며(`calendar`/`carbon-crisis`/`climate` 단위 테스트가 `hour`/`dayIndex` 스키마 불일치로 실패), 그 외 인구·연구 카드 개수 등 밸런스 수치 변경(`hud.spec.js`, `visual.spec.js`)과 `objectiveSetId` 진행 상태(`hex-scene.spec.js`)도 이번 변경과 무관하게 이미 깨져 있었다. 건드리지 않았다.
- OX 위젯의 위치·투명도·건축 독 접힘·확정 후 무장 해제는 스크린샷으로 직접 확인했다.
- 저장소 안전: git add/commit/push, 배포, 원격 변경을 전혀 수행하지 않고 로컬 변경만 보존했다.

### 추가 수정 — 독 접힘 애니메이션이 중간에 멈춰 "껍데기"가 남는 문제

- [x] 건축 독을 감싼 `#buildPanel` 전체(배경 포함)를 접도록 바꿨다 — 이전에는 독 목록/상세만 숨기고 패널 배경과 안내 문구(`#buildPendingNote`)가 남아 있어 "껍데기"처럼 보였다. 안내 문구는 제거하고 패널 자체를 여닫을 때 쓰던 것과 같은 슬라이드 기법(`build-panel--collapsed` 클래스, `opacity/visibility/transform`)으로 완전히 감춘다.
- [x] 패널을 연 직후 곧바로 칸을 클릭하면(사람이 실제로 하는 타이밍과 가깝다) `opacity`가 0.09 근처에서, `visibility`가 `visible`인 채로 멈춰 화면에 흐릿한 독 카드가 계속 보이는 경우를 재현했다 — 열림 전환과 접힘 전환이 겹치며 CSS 트랜지션이 끝까지 진행되지 않는 문제였다. 접힘 상태에 `transition:none`을 줘서 트랜지션에 기대지 않고 즉시 완전히 사라지도록 고쳤다.
- [x] `getComputedStyle`로 값을 직접 확인하고 스크린샷으로 재현·수정을 검증했다. 관련 회귀(`build-preview`, `hud`, `mobile`, `camera`) 재통과.

## 37차 수정 — 시설 레벨별 실제 3D 모델 교체 (kenney_city-kit-industrial 2.0)

원본 요청: "assets-source > archives에 kenney_city-kit-industrial_2.0 다운받아 둠. 각 단계마다 모델이 바뀌었으면 함" — 화력발전(building-n→m→i), 태양광(solar-panel-portrait→landscape→landscape-group), 원자력발전(chimney-small→medium→large), 순환냉각(detail-tank×1→×2 스케일→detail-tank-large), 데이터센터(skyscraper-a→c→e). 이후 대화 중 에너지저장(building-p→t→q)과 풍력(windmill-low→windmill-low 스케일업→windmill)도 같은 방식으로 추가 요청받았다.

- [x] **기존 구조 확인**: `FACILITY_ASSET_IDS[type]`에는 원래도 레벨별 3개 항목이 있었지만 렌더러(`CityAssetLoader.getFacilityGeometry`)가 항상 `[0]`만 읽어 레벨 2·3은 스케일만 키운 같은 모델이었다. `residential`/`factory`/`tidal`은 서로 다른 id가 이미 들어 있었지만 실제로 쓰인 적이 없었던 것도 확인했다 — 이번 요청 대상이 아니므로 겉모습이 바뀌지 않게 `[0]`을 3번 반복하도록 의도적으로 맞췄다(`assetRegistry.js` 주석 참고).
- [x] **에셋 파이프라인**: `assets-source/acquisition.json`·`manifest.json`에 `kenney-industrial2` 소스(이미 받아둔 `kenney_city-kit-industrial_2.0.zip`)를 등록하고, `selection.json`에 요청받은 GLB 17개(화력 3·원자력 3·태양광 3·순환냉각 1·에너지저장 3·풍력 2·데이터센터 2)를 추가해 `select-3d-assets.mjs` → `assets:optimize` → `assets:audit`를 정식으로 통과시켰다(총 45 → 62개 선별 GLB). `public/assets/licenses/ASSET_LICENSES.md`에 출처·레벨 매핑을 기록했다.
- [x] **레벨별 실제 지오메트리 로딩**: `CityAssetLoader.js`의 캐시 키를 시설 타입에서 실제 에셋 id로 바꿔, 한 타입이 레벨마다 다른 에셋을 참조해도(또는 여러 타입이 같은 에셋을 공유해도) 한 번만 로드하고 자연히 캐시를 공유하게 했다. `getFacilityGeometry(type, level)`/`getFacilityMaterial(type, level)`로 레벨을 받되 기본값 1이라 기존 호출부는 대부분 그대로 뒀다.
- [x] **레벨별 인스턴싱**: `CityScene3D.js`의 `facilityMeshes`(타입당 InstancedMesh 1개, 기존 그대로 유지)에 더해, 레벨의 실제 에셋이 1레벨과 다를 때만 `${type}:${level}` 보조 메시를 지연 생성하는 `extraLevelMeshes`를 추가했다. 보드에 없는 조합은 만들지 않는다. 고스트 미리보기·머티리얼 팔레트·통계(`materials.factory` 등 타입 단위 리포트)는 항상 1레벨(대표) 버킷을 그대로 참조해 기존 계약을 건드리지 않았다.
- [x] **드로우콜 예산 초과 발견 및 처리**: 37칸 대표 도시(모든 타입×모든 레벨) 최악의 경우 실측 24 → 33 → (풍력 추가 후) 35 draw calls. `docs/superpowers/plans/2026-08-30-cc0-asset-pipeline.md`가 명시적으로 `Map<type:level, InstancedMesh>` 방식을 draw-call 이유로 보류했던 과거 결정을 발견해 사용자에게 그대로 알리고, 예산을 24 → 36으로 올리는 것으로 최종 확인받았다(`docs/architectural-decisions/0003-...md`에 갱신 기록). 실기기 체감 저하 여부는 스크린샷·플레이로 별도 확인은 안 했고, 33~35 draw calls 자체가 여전히 가벼운 수준이라는 판단으로 진행했다.
- [x] **순환냉각·풍력의 "1·2단계 스케일만" 처리**: 순환냉각은 요청, 풍력은 사용자가 드로우콜 절감 목적으로 직접 제안해 채택 — 두 시설 모두 1·2단계는 같은 자산을 공유(스케일만 차등), 3단계만 실제로 다른 모델을 쓴다. 별도 다중 인스턴스/geometry 합성 없이 기존 매커니즘 그대로 처리된다.
- [x] **풍력 로터 정렬 확인**: 풍력은 회전 블레이드(`windRotorMesh`)가 기단 모델과 별도 레이어로 얹히는데, Y 오프셋이 `0.78 * level.scale`로 고정돼 있어 기단 모델이 바뀌면 위치가 어긋날 위험이 있었다. 스크린샷으로 직접 확인한 결과 `windmill-low`/`windmill` 모두 허브 위치가 잘 맞아 별도 보정 없이 그대로 뒀다.
- [x] `CityAssetLoader.initCityAssets`의 타입-성공/실패 집계 버그를 하나 발견해 고쳤다: 같은 1레벨 에셋을 공유하는 타입이 2개 이상이면(예: 순환냉각·조력 모두 `storageTank`) 로드 작업을 에셋 id로만 중복 제거하다 보니 먼저 순회된 타입에만 성공이 귀속되고 나머지 타입은 `status.loaded`에서 누락됐다. 로드는 에셋 id 단위로, 성공/실패 집계는 타입 단위로 완전히 분리해 해결했다.

### 최종 검증

- 직접 관련된 테스트(`asset-registry` 단위 23개, `perf` 27개, `assets` 9개, `motion` 8개, `build-preview` 10개, `visual` — 스냅샷 갱신 후 재확인)는 전부 통과했다. 스크린샷으로 레벨별 실제 모델 교체(화력·원자력·태양광·순환냉각·데이터센터·에너지저장·풍력)와 풍력 로터 정렬을 직접 눈으로 확인했다.
- 전체 회귀에서 이번 작업과 무관한 실패를 다시 확인했다: 이전에 이미 발견한 시(hour)→일(day) 리팩터링 관련 실패에 더해, 이번 세션 도중 `QuestDefinitions.js`가 실시간으로 바뀌는 것을 직접 목격했다(디스크 변경 알림) — 퀘스트/캠페인 시스템이 세션 중에도 계속 바뀌고 있어 `climate-quests`/`campaign-playthrough`/`objectives-ui` 등 대규모 회귀 실패가 새로 나타났다. `perf.spec.js`의 "rapid sequential facility placement" 테스트도 `questIndex=15`를 "허가 상한이 충분히 높은 퀘스트"로 가정했는데, 진행 중인 퀘스트 개편으로 permit 표가 바뀌며 도중에 허가가 막히는 것을 확인했다(9/9, "퀘스트 17" 언급 — 이 저장소의 현재 퀘스트 수 15개를 넘어섬). 이 부분은 손대지 않았다 — 진행 중인 다른 작업과 충돌하는 것으로 보이며, 무엇이 "충분히 높은 퀘스트"인지가 계속 바뀌는 상태라 지금 고쳐도 다시 깨질 가능성이 높다.
- 저장소 안전: git add/commit/push, 배포, 원격 변경을 전혀 수행하지 않고 로컬 변경만 보존했다.

## 38차 수정 — 연구 준비 4단계·퀴즈 재출제 방지·19단계 캠페인

- [x] 6개 기초 퀘스트와 8개 기후 퀘스트 사이에 `태양광 연구 기초 → 데이터센터 현대화 → 풍력 실증망 → 해안 조력 실증` 4단계를 추가했다. 기후 대응은 11~18, 41일 최종시험은 19단계로 이동했다.
- [x] 풍력·조력 준비 퀘스트는 발전량 표시가 아니라 소비시설로 실제 전달된 전력 경로를 2일 연속 확인한다. 공사 중인 데이터센터·조력 시설은 완료 조건에서 제외한다.
- [x] 이미 맞혀 연구 가속을 받은 문항은 다음 퀴즈에서 제외한다. 틀린 문항은 남고, 전 문항을 맞히면 명확한 완료 안내를 표시한다.
- [x] 11개 연구의 44개 퀴즈 선택지를 교정하고, 정답이 문장 길이만으로 드러나지 않는지 자동 검증한다.
- [x] 데이터센터 연구 목록에서 완료·진행 중 중복 카드를 제거했다. 시작 가능한 연구를 먼저 정렬하고 잠긴 연구에는 저채도·잠금 라벨·해금 조건을 표시하며, 전 연구 완료 빈 상태를 추가했다.
- [x] 시설 누적 허가를 19단계로 확장하고 이전 단계보다 한도가 낮아지지 않도록 최대값으로 병합한다. V8 저장은 V9로 마이그레이션해 도시·공사·연구·배터리를 보존하면서 새 퀘스트 번호로 이동한다.

### 최종 검증 (2026-09-02, 퀘스트 범위 한정)

- 사용자가 전체 회귀 비용을 줄이도록 요청해 건설·3D·시각·성능 전체 회귀는 실행하지 않았다.
- 퀘스트·연구·기후·시설 허가·V2~V9 저장 마이그레이션 단위 회귀 **115/115 통과**.
- 연구창·퀘스트창·기후 캠페인 브라우저 회귀 **30/30 통과**.
- `npm run build` 성공. 기존 Vite 단일 청크 크기 경고만 남아 있으며 빌드 오류는 없다.
- git add/commit/push, 배포, 원격 변경은 수행하지 않았다.

## 39차 수정 — 첫 확장 재생에너지 분기와 8단계 2차 확장

- [x] 첫 확장 선택을 실제 준비 퀘스트에 연결했다. 동부를 고르면 태양광, 서부를 고르면 풍력이 즉시 해금되고 7단계 연구 목표도 각각 `고효율 태양전지` / `풍력 예측 제어`로 바뀐다.
- [x] 8단계 `데이터센터 현대화`를 완료하면 첫 선택의 반대편 외곽 9칸과 반대 재생에너지가 함께 열린다. 동부 선행은 서부·풍력, 서부 선행은 동부·태양광으로 이어지며 보드는 28칸에서 37칸으로 확장된다.
- [x] 9단계 실증망도 두 경로를 대칭화했다. 첫 선택이 동부면 풍력 연구·실제 송전을, 서부면 태양광 연구·실제 송전을 2일 연속 검증한다.
- [x] 퀘스트 카드·퀘스트 전체 목록·상단 미션·보상 알림·시설 잠금 안내가 저장된 `expansion.firstChoice`를 기준으로 같은 분기 문구를 사용한다.
- [x] 첫 확장 모달에 선택 발전시설 해금과 8단계의 반대편 개방 시점을 명시했다.

### 최종 검증 (2026-09-02, 퀘스트 범위 한정)

- 퀘스트 분기 단위·실제 UI 회귀와 기존 1~19단계 퀘스트·기후 캠페인 회귀 **48/48 통과**.
- 보상 클릭 테스트는 실시간 일일 정산이 중간에 개입하지 않도록 해당 테스트에서만 Game Clock을 일시정지해 원자성을 고정했다.
- `npm run build` 성공. 기존 Vite 단일 청크 크기 경고만 남는다.
- 전체 게임 회귀는 사용자 요청에 따라 실행하지 않았고, git add/commit/push·배포·원격 변경도 수행하지 않았다.

## 40차 수정 — 시설 모델 세부 조정과 미사용 에셋 정리

37차에서 만든 레벨별 실제 모델 교체 구조를 계속 쓰면서, 대화 중 세부 매핑을 여러 차례 조정받았다.

- [x] **주거지**: 기존 `building-type-a` 단일 모델(스케일만 차등)에서 실제 레벨별 교체로 바꿨다 — 1단계 `building-type-a` → 2단계 `building-type-c`(이미 선별돼 있던 `residential.house3`를 재사용) → 3단계 `building-type-f`(신규 선별, `residential.house4`).
- [x] **조력발전**: 기존 산업 킷 저장탱크 재사용에서 사용자가 새로 준비해 둔 "Space Bits"(Kay Lousberg, itch.io, CC0) 팩의 `Cargo.glb → Cargo Depot A.glb → Cargo Depot-cRtW9KaGs0.glb`로 교체했다. 이 아카이브는 원본 `License.txt`가 없어 `select-3d-assets.mjs`가 실패했다 — 사용자에게 출처를 확인받은 뒤(Kay Lousberg / CC0-1.0), 원본 라이선스가 없는 소스는 `manifest.json` 정보로 라이선스 노트를 생성해 대신 보존하도록 파이프라인 스크립트에 `synthesizeLicenseNote` 폴백을 추가했다(Kenney 외 출처를 쓰는 향후 팩에도 재사용 가능).
- [x] **공장**: 레벨별 실제 모델 교체 여부를 별도로 요청받지 않아 처음부터 단일 모델(스케일만 차등)로 유지하되, 모델 자체는 industrial 2.0 kit의 `building-s.glb`로 바꿨다(기존 1.0 kit `building-b.glb`에서 교체, id/타깃 파일명은 유지).
- [x] **에너지저장**: 처음에는 요청대로 `building-p → building-t → building-q` 레벨별 실제 교체로 구현했으나, 이후 사용자가 드로우콜 절감을 위해 `shipping-container-b` 단일 모델(스케일만 차등)로 되돌리도록 정정했다. 레벨별 교체용으로 선별했던 3개 GLB는 이번 정리 대상에 포함했다.
- [x] **미사용 에셋 정리**: 이번 세션의 레벨별 교체 이전부터 이미 아무 시설도 참조하지 않던 것(주거지 house2/apartment1-2, 상업 shop1-2/medium2)과, 레벨별 교체로 대체되며 새로 미사용이 된 것(공장 factoryMedium/factoryLarge, 화력의 옛 chimney, 에너지저장의 building-p/t/q, Quaternius `energy.solarSmall/solarLarge/windBase`)을 `assets-source/selection.json`·`src/assets/assetRegistry.js`·`public/assets/buildings/`에서 모두 제거했다(60 → 52개 선별 GLB). 더 이상 어떤 모델도 제공하지 않는 Quaternius Ultimate Space Kit·Farm Buildings Pack 항목은 `ASSET_LICENSES.md`에서도 제거하되, 무엇을 왜 정리했는지는 각주로 남겼다.
- [x] `assets:select`/`assets:optimize`/`assets:audit` 파이프라인을 각 변경마다 다시 통과시켰고, 드로우콜 최악 시나리오가 24 → 36 → 38(실측)로 계속 올라가 예산을 40으로 다시 올렸다(레벨별 실제 교체 대상이 5개에서 주거지·조력·풍력을 더해 8개로 늘고, 에너지저장이 다시 빠지며 net 증가).

### 최종 검증

- `asset-registry` 단위 테스트를 매 변경마다 다시 맞춰 23/23 통과(선별 개수·라이선스 각주·레벨별 스왑 대상 목록 갱신 포함). `perf`·`assets`·`motion`·`build-preview`·`camera` 관련 스위트 재통과, `visual` 스냅샷은 이번 라운드에서는 갱신 없이 그대로 통과했다.
- 스크린샷으로 주거지·조력·에너지저장·공장의 레벨별(또는 단일) 실루엣을 직접 확인하고, `window.__getCityAssetStatus()`로 11개 시설 전부가 폴백·에러 없이 로드됨을 확인했다.
- 전체 회귀에서 이번 작업과 무관한 실패가 계속 나타난다 — 동시 진행 중인 퀘스트/캠페인 개편(38~39차, 15 → 19단계)이 원인으로 보이는 `퀘스트 17/19` 언급, `LEVEL 1/19` 등을 직접 확인했다. 손대지 않았다.
- 저장소 안전: git add/commit/push, 배포, 원격 변경을 전혀 수행하지 않고 로컬 변경만 보존했다.

### 화력발전 3단계 모델 수정 (2026-09-02)

- [x] 40차에서 화력발전 3단계에 `building-i.glb`를 넣었던 것이 사용자가 실제로 요청한 `building-l.glb`와 달라 바로잡았다. `assets-source/selection.json`의 `industrial.thermalLarge`, `src/assets/assetRegistry.js`, `ASSET_LICENSES.md`의 화력발전 레벨 실루엣 문구를 모두 `building-l.glb` / `thermal-l.glb`로 갱신했다.
- [x] `assets:select` → 기존 `thermal-i.glb` 삭제 → `assets:optimize`(162424 → 49628 bytes) → `assets:audit`(52개 선별 GLB, 그대로) 순서로 파이프라인을 다시 통과시켰다.
- [x] `asset-registry`·`perf`·`assets` 스위트 재실행 42/1 통과 — 유일한 실패는 이번 변경과 무관한 `퀘스트 17/19` 허가 한도 문구발 `residential` 클릭 타임아웃(동시 진행 중인 퀘스트 개편)이었다.
- [x] 화력발전 1·2·3단계를 동시에 배치한 스크린샷으로 세 모델이 서로 다른 실루엣임을 직접 확인했다.

## 41차 수정 — 2026-09-02 전체 리뷰 후 전면 개선 (2026-09-02~03)

원본 요청: "AI_City_Webgame_V3 리뷰해줘" → `docs/game-review-2026-09-02.md`(High 7·Medium 25·Low 다수) → "발견된 문제점들 전부 고쳐줘". 계획은 `docs/superpowers/plans/2026-09-02-review-remediation.md`(9개 작업), 각 작업마다 구현 → 독립 리뷰 → 수정 라운드를 거쳤고 마지막에 전체 브랜치 리뷰와 한 번의 수정 묶음을 돌렸다.

- [x] **진행 막힘 3건**: 19단계 최종시험에서 보드가 잠기던 문제(`stageForQuest`가 REPORT를 반환하지 않고, 로드 시 기존 저장의 REPORT도 보정), 6단계 보상 직후 새로고침 시 확장 선택이 유실되던 문제(`expansionChoicePending` + 부팅 시 재요청), v8→v9 마이그레이션 소프트락(준비 퀘스트·시설·Lv.2 허가·2차 확장을 보충하고 기후 상태를 `briefing`으로 정규화).
- [x] **상태·저장**: 로드 후 `refreshMetrics`, `pagehide`/숨김 시 저장 플러시, `safeStorage`(차단된 저장소에서도 부팅), 저장 실패 시 경고+1회 토스트, `-newer` 저장 보호, UI 모듈 상태 `GAME_RESET` 초기화, `core/ConstructionProject.js`로 계층 역전 해소, 점수·비용 상수화.
- [x] **밸런스**: 발전소 탄소·물이 실제 송전량/가용량 비율을 따름(유휴 하한 `GENERATION_IDLE_EMISSION_RATIO`), 물 한도는 "행사 직전 사용량 이하 유지"(가뭄·건조 위기, 열돔은 물 제한 없음, 위반 허용 3일), 보고서 시험 길이 41일, 7단계 서부 분기 풍력 허가, 죽은 허가행·목표 세트 계층·`STAGES.CRISIS/CONCEPTS` 제거, 필수시설 0개 = 공급 100%, 탄소 경보 마일스톤 회복, 연구 취소 시 퀴즈 크레딧 반환.
- [x] **UI**: 모달 우선순위 큐(게임오버·운영 위험이 다른 모달을 덮되 복구), 모달 접근성(dialog·포커스 트랩·Escape·포커스 복원), 키보드로 보드 조작(방향키·Home·Enter·Escape, `#srAnnouncer`), lucide 아이콘 17종 등록 + 등록 검사 테스트, `refreshIcons(root)` 범위 한정, aria-live 정리, 퀘스트 진행률 단일 소스(`questProgressFraction`), 긴급 지원 버튼을 퀘스트 패널에도, 효과음/배경음 분리, 용어 통일(퀘스트), 44px 터치 타겟, 모션 축소 반영, 이스케이프, 죽은 CSS 정리.
- [x] **3D/성능**: 새 방문이 정산에 끊기지 않음, 시계 rAF 루프가 공사가 없거나 정지 시 유휴, 공사 배지 변화 없을 때 DOM 쓰기 생략, 최악 드로우콜 실측 47·예산 49(ADR-0003), 해제 경로 완성(테스트 훅), 로딩 화면이 실제 에셋 진행률을 따름.
- [x] **문서·저장소·빌드**: `gameplan.md`·`tech.md`를 현재 설계로 재작성(원래 6단계 설계는 `## 설계 변천`에 보존), `assets-source/archives`·`.superpowers` git 추적 해제(679개, 파일은 디스크에 유지), 벤더 청크 분할(three 653kB·chart 지연 로딩), `playwright.config.js`가 `PW_PORT`를 지원.
- [x] **테스트**: 기존 실패 26건 복구, 약화됐던 테스트 원복(5단계 수치, 서부 분기 1→19 기준 캠페인, 실제 배치 경로, HUD 1→6 실플레이, 풍력 삼각형 수), 3건의 실제 버그 수정(잠긴 태양광/풍력 카드 안내, 정전 원인 목록의 옛 이벤트 id, `lowWind` 죽은 규칙).

### 최종 검증 (2026-09-03)

- `PW_PORT=3177 npx playwright test --reporter=line --retries=0`: **606/606 통과** (13.9분, 아이콘 경고 0, 페이지 오류 0).
- `npm run build`: 성공, 4개 JS 청크(three 652.89kB, index 377.54kB, chart 203.30kB 지연, vendor 31.10kB).
- 환경 주의: 이 세션 동안 VS Code 도우미 프로세스가 `localhost:3000`을 점유해 `pyx.kr`로 리다이렉트했다. 테스트는 `PW_PORT=3177`로 실행했다.
- 범위 밖으로 남긴 것: `CityScene3D.js`(≈2,000줄)·`StageModals.js` 모듈 분할, 밸런스 플레이테스트(급전 기반 탄소로 11단계 목표가 8로 낮아짐, 노동력 부족이 발전소 탄소를 더는 낮추지 않음).
- git add/commit/push는 수행하지 않았다(사용자가 `2aaaf8c`, `a25f1c4`로 중간 커밋). 작업 트리에는 남은 수정과 `git rm --cached` 스테이징(679건)이 있다.

## 42차 수정 — 시설 선택 즉시 건설 패널 접힘, 취소 시 배치 모드 해제 (2026-09-03)

원본 요청: "건물을 클릭하자마자 탭이 닫혀야 편한듯. 그래야 건설할 때 어디 설치할지 잘 보이잖아", "건설 미리보기에서 건설 취소를 누른 뒤에도 마우스가 일반 마우스로 돌아가는 게 아니라 건축모드네".

- [x] 독에서 시설 카드를 누르면 즉시 `#buildPanel`이 접혀(`build-panel--collapsed`, 패널은 `hud-panel-active` 유지) 보드가 드러난다. 데스크톱·모바일 공통. 칸을 골라 확정(O)·취소(X)하면 패널이 돌아온다(`DockView.js` `collapsedForPlacement`).
- [x] 접힌 상태에서 건설 버튼을 다시 누르면 패널이 닫히는 대신 다시 펼쳐진다. `WorldHud.toggleHudPanel`이 닫기 전에 `HUD_PANEL_CLOSE_REQUESTED`를 발행하고, 독이 `preventClose()`로 가로채 펼친다. 카드를 키보드로 골랐다면 포커스를 보드(`#cityGrid`)로 옮긴다.
- [x] 취소(X)도 확정(O)과 같은 규칙으로 배치 무장을 해제한다(`GridView.js`가 `BUILD_PLAN_CLEARED`에서 `facilityArmed=false`). 취소 뒤 마우스를 움직여도 고스트가 따라다니지 않고, 다시 지으려면 시설을 다시 고른다.
- [x] 테스트: `build-preview.spec.js`에 데스크톱·모바일 접힘/펼침/확정/취소 흐름과 취소 후 무장 해제 테스트 추가, `hud.spec.js`의 "선택 후 팔레트 유지" 테스트를 새 계약으로 갱신. 영향 범위 14개 스위트 **147/147 통과**(9.8분, `PW_PORT=3177`).

## 43차 수정 — /improve-game UX·상호작용·HUD·카메라·모바일 (2026-09-03)

원본 요청: `/improve-game UX threejs interaction HUD camera selection feedback mobile usability`. 심층 점검(진단표 50/65)에서 초점 영역 개선안 7개를 뽑았고 사용자가 전부 승인했다.

- [x] **터치 탭 판정 완화**: 손가락 탭은 5~15px 흔들리는데 마우스용 7px 임계값을 그대로 써서 8px부터 탭이 무시됐다(실측). 터치 포인터는 `CITY_CAMERA.TAP_THRESHOLD_TOUCH_PX`(14)까지 탭으로 인정한다(`CameraController.js`).
- [x] **세로 화면 줌 한계**: 핀치로 셀 하나가 화면을 덮을 만큼 확대되던 것을 `MIN_DISTANCE_PER_GRID_PORTRAIT`(0.95)로 막았다. `__getCityCameraState()`가 `minDistance/maxDistance/atDefault`를 노출한다.
- [x] **카메라 되돌리기**: 기본 시점을 벗어났을 때만 보드 왼쪽 아래에 "⌖ 시점 초기화" 칩(`#cityRecenterBtn`)이 나타나고, 보드 칸이 아닌 바닥을 350ms 안에 두 번 누르면(더블탭/더블클릭) 시점이 돌아온다. 36차에서 뺀 상시 버튼은 되살리지 않았다.
- [x] **빈 칸 탭 = 선택 해제**: 건설 모드가 아닐 때 빈 칸을 누르면 매번 "건설 메뉴를 먼저 여세요" 토스트가 떴다. 이제 선택 링을 풀고, 안내(`BOARD_TAP_COPY`)는 세션당 한 번만 나온다(리셋 시 초기화).
- [x] **배치 안내 힌트**: 시설 카드를 고르면 패널이 접히는 42차 흐름에서 첫 사용자가 다음 행동을 모를 수 있어, 첫 건설을 확정하기 전까지 "빈 칸을 눌러 {시설} 배치" 힌트(`UI_FEEDBACK.PLACEMENT_HINT_MS`)를 띄운다.
- [x] **탭 즉각 피드백**: 칸을 누르면 `BOARD_CELL_TAPPED`가 발행되고, 누른 칸이 220ms 흰 링으로 반짝이며(`tapFlashIndex` 통계), 클릭음과 터치 시 10ms 진동(`navigator.vibrate`)이 난다. 키보드 Enter도 같은 경로.
- [x] **모바일 퀘스트 스트립**: 모바일 상단 바 아래에 현재 퀘스트(레벨·제목·진행률)를 항상 보여주는 `#questStrip`을 추가했다. 퀘스트 패널과 같은 렌더 경로(노드 배열)로 값을 받고, 누르면 퀘스트 패널이 열리며 패널이 열린 동안은 숨는다. 예보 스트립은 그 아래(148px)로 내려갔다. 데스크톱에서는 표시되지 않는다.
- [x] 테스트: `board-tap.spec.js`(신규), `camera.spec.js`·`mobile.spec.js`·`build-preview.spec.js`·`hud.spec.js`에 총 9개 추가. 모바일 시각 스냅샷 3장(`world-hud-mobile-default/build`, `city-kit-mobile`)은 스트립 추가로 갱신했다 — 갱신 전 기준은 옛 HUD(`0/h`, 옛 리셋 버튼)를 담은 채 허용 오차 안에서만 통과하던 것이었다.

## 44차 수정 — 도시 조명·운영 모드 제거, 모바일 상단 바 1줄 (2026-09-03)

원본 요청: (1) 도시조명 낮·노을·밤 제거 + 코드정리, (2) 각 건물의 운영모드 제거 + 코드정리, (3) 모바일 퀘스트 창(스트립) 폭을 제목에 맞게 축소, (4) 모바일 상단 탭 1줄(브랜드 마크만 남기고 문구 숨김, 전력 생산·저장 상하 묶음, 에너지 값 뒤 E 제거), (5) 상단 탭의 CO₂ 세로 정렬과 모바일에서 CO₂가 안 보이던 문제.

- [x] **운영 모드 제거**: `OPERATION_MODES`·`operationModeDefinition`·`availableOperationModes`(OperationDefinitions.js에는 `BATTERY_POLICIES`만 남김), 자동 수요반응(`applyAutomaticOperationModes`)과 그 전용 연구 `demandResponse`(퀴즈 4문항 포함, 연구 11→10종), 셀의 `operationMode` 필드(`normalizeCell`이 옛 저장의 키를 버림), 강화 공사의 모드 보류/복원, `decisionCounts.modeChanges/automaticModeChanges`, `OPERATION_MODE_CHANGED` 이벤트, 시설 상세의 운영 모드 컨트롤과 모드 변경 예측 모달, 연구의 `mode_paused` 상태를 모두 제거했다. 절전·증산·운영 모드를 권하던 문구는 우선순위·배터리 정책·순환냉각·연구로 바꿨다. 남은 물 절감 수단은 순환냉각 인접뿐이며 기준 캠페인은 그대로 통과한다.
- [x] **도시 조명 제거**: `WorldLightingManager.js`·설정 패널의 GRAPHICS 블록·`WORLD_LIGHTING_MODES` 상수·`setVisualWorldHour`/`__setWorldHourForTest`/`__getWorldLightingMode` 훅과 함께, 낮/노을/밤 조명 단계표·시간별 하늘 상태(`getSkyState`/`getWorldPhase`)·밤 전용 창문 조명 레이어를 제거했다. 씬은 `WORLD_DAY_LIGHTING` 한 가지 조명으로 고정되며 밝은/어두운 테마 토글은 그대로다. 최악 드로우콜 실측 47→46(예산 49 유지, ADR-0003 갱신). `day-night-scene.spec.js`와 하늘 스냅샷 3장 삭제.
- [x] **모바일 상단 바 1줄**: `.world-status`를 flex 한 줄로 바꾸고 브랜드 문구(`.brand-copy`)를 숨겼다. 전력 여유와 배터리 저장량은 그리드 한 칸을 상하로 나눠 쓴다(각 21px, 합쳐 44px). 전력·배터리 값의 `E` 단위를 뗐다(툴팁에는 유지). 시간 조절 버튼은 한 줄에 맞게 36px. 360px 폭에서도 한 줄로 들어간다.
- [x] **CO₂**: `.sim-metric-icon`을 inline-flex 중앙 정렬로 바꿔 텍스트 라벨(CO₂)이 SVG 아이콘과 같은 높이에 놓이게 했고, 모바일에서 `.simulation-hud small{display:none}`이 CO₂ 라벨까지 숨기던 것을 `.sim-date small`로 좁혔다.
- [x] **퀘스트 스트립**: 폭을 내용에 맞춤(`width:auto`, 제목 최대 52vw). 한 줄 상단 바에 맞춰 76px, 예보 스트립은 122px로 이동.
- [x] 테스트: 모드·조명 전용 테스트 삭제, 픽스처의 `operationMode` 키 제거, 단위 기대값(`+1 E`→`+1`) 갱신, 터치 타겟 테스트는 시간 버튼 36px·전력/배터리 합산 규칙으로 조정, 모바일 스냅샷 3장 갱신.

## 45차 수정 — 건물 회전, 방향별 발전량, 풍향·조차 환경 (2026-09-03)

원본 요청: 건설할 때 회전 화살표 아이콘으로 건물을 돌릴 수 있게; 태양광·풍력은 방향에 따라 발전량이 달라지고 방향별 발전량을 보는 모달 추가; 태양광 최적 방향은 남쪽 고정, 풍력은 칸마다 다르게; 조력은 내륙에 못 짓고 기존 "이득 위치" 표시 대신 해안 칸의 조수간만의 차를 표시; 이 값들은 새 게임마다 무작위. 확인받은 결정: 8방위 45° 회전, 풍력 기본 방향은 북쪽(학생이 모달을 보고 직접 돌려야 최대 출력).

- [x] **환경(`state.environment`)**: 새 게임마다 32비트 씨앗을 뽑아 `core/Environment.js`의 `createEnvironment(seed)`로 칸별 풍향(주풍향 ± 2칸 흔들림)과 해안 칸(최외곽 링 18칸)의 조차(2~8m, 0.1m 단위)를 만든다. 저장에 유지되며(v10 마이그레이션), `__setEnvironmentSeed(seed)`·`__getEnvironmentAt(index)` 훅으로 테스트가 고정한다.
- [x] **방향 규칙(`DIRECTION_RULES`)**: 셀의 `rotation`(0~7, `FACILITY_DIRECTIONS` 인덱스). 태양광 최적 = 남(고정), 풍력 최적 = 그 칸의 풍향. 최적과의 편차(45° 칸 수 0~4)에 따라 출력 배율 태양광 1/.92/.72/.5/.38, 풍력 1/.9/.7/.5/.35. 기본 회전: 태양광 남, 풍력 북. 동부 태양광·서부 풍황 구역 보너스는 그대로 곱해진다.
- [x] **조력(`TIDAL_RULES`)**: 해안 칸에만 건설(`coastal_required`), 옛 3곳 보너스 입지(`TIDAL_SITE_COORDINATES`) 삭제. 출력 배율 = 조차/5m(0.5~1.5). 미리보기·시설 상세에 `조차 6.3m · 출력 126%`를 표시한다.
- [x] **건설 위젯**: O/X 옆에 회전(`rotate-cw`)과 방향(`compass`, 태양광·풍력만) 버튼. 회전은 계획 항목의 `rotation`을 45°씩 돌리고(보드 포커스 시 `r` 키도 가능) 고스트가 즉시 돌아간다. 방향 모달은 8방위 각각의 출력 %와 최적 표시, 풍력은 "이 칸의 바람" 안내, 클릭하면 계획에 반영. 완공 후에는 방향을 바꾸지 않는다. 건설 후 예상 바에 `방향 남 · 출력 100%` / `조차 …`가 붙는다.
- [x] **계층 정리**: 육각 기하(`core/HexGrid.js`)와 환경 생성(`core/Environment.js`)을 core로 옮기고 systems 모듈은 재수출만 한다(core→systems import 0건).
- [x] 테스트: `unit/environment.spec.js`, `unit/state-v10.spec.js`, `direction.spec.js`(7) 신규; 기준 캠페인·퀘스트 가능성·구역 테스트는 씨앗 20400134로 고정하고 풍력은 최적 방향으로 짓도록 갱신. 시각 스냅샷은 허용 오차 안(태양광 기본 남향 180° 회전이 판 배열 가장자리만 바꿈)이라 갱신하지 않았다.
